# انتقال ابزارها به تنظیمات دیگر و اصلاح سلامت پایگاه داده

## دانلود و نصب

**فقط اصلاحی — ۵ فایل مشترک وب، هر بسته حدود ۲۷ کیلوبایت.** بسته‌های کامل، exe، شمارهٔ نسخه، داده‌ها و تنظیمات اتصال بازسازی یا جایگزین نشده‌اند.

- [اصلاحی سایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-settings-health.zip)
- [اصلاحی دسکتاپ](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-settings-health.zip)
- [SHA256](SETTINGS-HEALTH-SHA256SUMS.txt) · [فهرست فایل‌ها](scripts/settings-health-files.json)

این بسته **پس از report-tools** نصب شود. ترتیب کامل اصلاحی‌های اخیر:

`Release_V1.0 → svg-responsive → student-workflow → preview-navigation → fast-ui → mobile-hubs → report-tools → settings-health`

۱. از نصب و داده‌ها پشتیبان بگیرید.
۲. سایت: محتویات `site-update-v4.152.0` را در ریشهٔ نصب موجود ادغام و فایل‌های هم‌نام را جایگزین کنید.
۳. دسکتاپ: برنامه را کاملاً ببندید، محتویات `SchoolDeskPro/www` را با پوشهٔ `www` نصب ادغام کنید و برنامه را دوباره اجرا کنید. خود پوشهٔ نصب را حذف نکنید.
۴. اگر هاست OPcache را بدون بررسی تغییر فایل نگه می‌دارد، آن را بازنشانی کنید و صفحه را تازه کنید.

نصب اصلاحی، عملیات تبدیل موتور، پاکسازی رکوردها، VACUUM یا OPTIMIZE را خودکار اجرا نمی‌کند. فایل‌های اصلی همگام‌سازی سایت و دسکتاپ متفاوت‌اند و در این بسته جایگزین نشده‌اند؛ کلیدها و اطلاعات اتصال محفوظ می‌مانند.

## ۱. تنظیمات دیگر

سه ورودی مستقل زیر از سایدبار و فهرست انتخاب کاشی‌های هدر حذف و به تب‌های **تنظیمات دیگر** منتقل شدند:

- همگام‌سازی با سایت
- سلامت پایگاه داده
- مدیریت مدیران

چهار تب قبلیِ لاگ فعالیت، مایگریشن، پشتیبان‌گیری و مستندات API حفظ شده‌اند؛ مدیر ارشد در مجموع هفت تب دارد. انتخاب‌های قدیمیِ سه کاشی منتقل‌شده، هنگام نمایش به یک کاشی «تنظیمات دیگر» تبدیل می‌شوند؛ ردیف تنظیمات کاربر در دیتابیس بازنویسی نمی‌شود.

مجوزها حفظ شده‌اند و تب‌های غیرمجاز نشان داده نمی‌شوند:

- سلامت پایگاه و مایگریشن: `system_settings`
- مدیریت مدیران: نقش مدیر ارشد طبق کنترل صفحهٔ اصلی
- لاگ: `view_logs`؛ پشتیبان‌گیری: `manage_backups`
- همگام‌سازی: در سایت فقط حساب فعال مدیر ارشد؛ در دسکتاپ مطابق کنترل قبلی، مدیر واردشده

پنهان‌شدن تب جایگزین کنترل سمت سرور نیست؛ کنترل‌های خود صفحه‌ها همچنان برقرارند. آدرس‌های قدیمی برای سازگاری حذف نشده‌اند، ولی دیگر منوی مستقل برای این سه ابزار وجود ندارد. قاب مشترک موبایل، انتخاب تب و ناوبری قبلی حفظ شده‌اند.

## ۲. معنی هشدار موتور/کلیشن و علت اشکال

**InnoDB** موتور جدول در MySQL/MariaDB است. **utf8mb4** مجموعه‌نویسهٔ یونیکد در MySQL است؛ **collation** قواعد مقایسه و مرتب‌سازی متن را تعیین می‌کند.

SQLite سازوکار متفاوتی دارد؛ جدول SQLite نه InnoDB است و نه دارای کلیشن نام‌گذاری‌شدهٔ MySQL مانند `utf8mb4_unicode_ci`. این تفاوت به‌خودی‌خود خطای داده یا نیازمند تبدیل نیست.

اشکال قطعی در کد قبلی بازتولید شد: تشخیص اتصال SQLite درست انجام می‌شد، ولی در انتهای گزارش، شرط‌های «غیر InnoDB» و «غیر utf8mb4» **بدون بررسی نوع اتصال** به همهٔ جدول‌ها اعمال می‌شدند. نتیجه دقیقاً فهرست بلند همهٔ جدول‌های SQLite بود.

اصلاح انجام‌شده:

- در SQLite مقایسه با موتور/کلیشن MySQL انجام نمی‌شود و توضیح درست نوع اتصال نمایش داده می‌شود.
- در MySQL هشدار واقعی MyISAM یا کلیشن غیر utf8mb4 همچنان گزارش می‌شود؛ هشدارها به‌صورت عمومی مخفی نشده‌اند.
- فرادادهٔ ناقص «نامشخص» است، نه سالم یا الزاماً نامنطبق.
- اگر نوع اتصال قابل تشخیص نباشد، عملیات متوقف می‌شود؛ سیستم با فرض MySQL دست به تغییر نمی‌زند.
- اسکن کامل SQLite، `PRAGMA quick_check` را نیز اجرا و نتیجه را صریح نشان می‌دهد.

**این پیام در SQLite دلیل تبدیل موتور یا تغییر کلیشن نیست.** برای MySQL واقعی، تفاوت موتور/کلیشن می‌تواند نیازمند بررسی باشد؛ تبدیل به پشتیبان، ارزیابی سازگاری و زمان کم‌ترافیک نیاز دارد. این اصلاحی هیچ `ALTER ... ENGINE` یا تبدیل مجموعه‌نویسهٔ خودکاری اجرا نمی‌کند. از این محیط به پایگاه دادهٔ نصب شما وصل نشده‌ایم و سالم‌بودن تمام داده‌های آن را ادعا نمی‌کنیم.

## ۳. اصلاحات بهینه‌سازی و گزارش‌دهی

### ایندکس‌ها

در SQLite نام ایندکس در کل پایگاه یکتا است. کد قبلی برای `students` و `reports` نام یکسان `idx_year_class` می‌ساخت؛ `CREATE INDEX IF NOT EXISTS` ممکن بود برای جدول دوم هیچ کاری نکند، ولی شمارنده آن را «ایجادشده» حساب می‌کرد.

اکنون:

- نام ایندکس SQLite شامل نام جدول است؛
- بعد از ساخت، وجود پوشش واقعی ستون‌ها بررسی می‌شود؛
- ایندکس هم‌ارز با نام دیگر یا با ستون‌های ابتدایی منطبق شناسایی می‌شود و دوباره ساخته نمی‌شود؛
- ایندکس جزئی، عبارتی، با ترتیب نامناسب یا با پیشوند ناقص، بی‌دلیل معادل اعلام نمی‌شود؛
- خواندن ناموفق اطلاعات ایندکس، «از قبل موجود» گزارش نمی‌شود؛
- اجرای مجدد دکمهٔ ایندکس‌گذاری در آزمون ایندکس تازهٔ تکراری نساخت.

ساخت ایندکس رکوردهای دانش‌آموز و نمره را تغییر نمی‌دهد، ولی می‌تواند زمان و فضای دیسک مصرف کند. فقط پس از پشتیبان‌گیری و در زمان کم‌ترافیک اجرا کنید؛ افزایش سرعت مشخص یا چندبرابری تضمین نمی‌شود.

### سبک‌ترشدن نمایش اولیه

نمای اولیه فراداده و ایندکس‌ها را می‌خواند؛ شمارش دقیق تمام جدول‌ها و بررسی‌های سنگین یتیم/تکراری به دکمهٔ **اسکن کامل (فقط خواندنی)** منتقل شده‌اند. مقدار بررسی‌نشده با «اسکن نشده» مشخص است، نه عدد صفر یا وضعیت سالم.

در پایگاه آزمایشی هر دو توزیع، رهگیری واقعی SQL نشان داد:

| وضعیت | تعداد کوئری شمارش کامل جدول SQLite |
|---|---:|
| نمای اولیه | ۰ |
| اسکن کامل اختیاری | ۵۷ |

این مقایسهٔ تعداد کوئری است، نه ادعای زمان پاسخ ثابت یا معیار سرعت روی مدرسهٔ شما. فرادادهٔ ستون‌ها و ایندکس‌ها نیز در همان درخواست دوباره استفاده می‌شود.

### عملیات نگه‌داری

- حجم SQLite بر اساس کل صفحه‌های فایل گزارش می‌شود؛ فضای آزاد جداگانه نمایش داده می‌شود. حجم مستقل ایندکس‌ها که در این مسیر قابل اندازه‌گیری نیست، عدد ساختگی صفر ندارد.
- پاسخ MySQL به ANALYZE/OPTIMIZE بررسی می‌شود؛ صرف نداشتن استثنای PDO برای اعلام موفقیت کافی نیست.
- پاکسازی، تعداد واقعاً حذف‌شده را گزارش می‌کند و خطای بررسی/حذف به‌عنوان موفقیت کامل پنهان نمی‌شود.
- در حذف تکراری‌ها، وابستگی‌های شناخته‌شدهٔ `student_id`، `user_id` و `class_id` در جدول‌ها محافظت می‌شوند، از جمله حضور و غیاب، تگ و صندلی؛ خطای بررسی اجازهٔ ادامهٔ حذف نمی‌دهد. کلاس با تعریف متفاوت یا وابستگی باقی می‌ماند.

پاکسازی داده و بازسازی فیزیکی همچنان کار مدیریتیِ صریح با POST، CSRF و مجوز است؛ بازکردن تب آن‌ها را اجرا نمی‌کند. پیش از این کارها پشتیبان لازم است. بررسی عمومی وابستگی‌ها جای ممیزی افزونه‌های سفارشی با ساختار ناشناخته را نمی‌گیرد.

## ۴. آزمون‌ها و شواهد

- **۶۵ بررسی PHP برای سایت و ۶۵ برای دسکتاپ:** هفت تب، انتخاب سمت سرور، حذف منوها، تبدیل انتخاب قدیمی کاشی‌ها، نقش‌ها، نبود هشدار کاذب SQLite، اسکن کامل، شمارش SQL، CSRF، جلوگیری از نوشتن با GET، بازتولید برخورد نام ایندکس، پوشش واقعی هر دو جدول، اجرای مجدد، حفظ دانش‌آموزان، یکپارچگی SQLite و حفاظت از کلاس‌های دارای وابستگی/تعریف متفاوت.
- طبقه‌بندی MySQL با فرادادهٔ آزمایشی بررسی شد: InnoDB/utf8mb4 سالم، MyISAM/utf8 نامنطبق، مقدار تهی نامشخص و پاسخ خطای نگه‌داری ناموفق. **سرور واقعی MySQL در این اجرا آزموده نشده است.**
- **۷۷ بررسی مرورگر برای هر توزیع:** ۲۰ چیدمان iframe واقعی شامل سه تب جدید و اسکن تفصیلی، در عرض‌های ۳۲۰، ۳۹۰، ۷۶۸، ۱۰۲۴ و ۱۴۴۰؛ کلیک واقعی بین سه تب نیز موفق بود. بیرون‌زدگی افقی یا فرم بریده‌شده در این حالات مشاهده نشد.
- Axe فقط برای کنترل‌های تبِ تغییرکرده نمونه‌برداری شد؛ تخلفی گزارش نکرد. این نتیجه ممیزی کامل فرم‌های قدیمی مدیران/همگام‌سازی یا گواهی AAA نیست.
- **هر ۳۱ مجموعه‌آزمون عمومی در هر دو توزیع موفق است.** دو قرارداد آزمون قدیمی با انتقال درخواستی هماهنگ شدند: تعداد کاشی‌ها ۲۰→۱۷ و تشخیص دقیق پیوند `settings.php` به‌جای تطبیق اشتباه با `other-settings.php`. سه نماد قدیمی SVG برای سازگاری در کتابخانه باقی‌اند، نه به‌صورت منو.
- **۱۲۸ بررسی بازیابی کارنامه‌ها روی سایت نیز دوباره موفق بود.**
- پنج آزمون بسته‌بندی جدید و سه مجموعهٔ پنج‌تایی بسته‌بندی‌های قبلی موفق بودند؛ هیچ ZIP تاریخی تغییر نکرده است.

اجرای PHP با SQLite آزمایشی و مرورگر Chromium بوده است. تست چیدمان HTML واقعی صفحه‌ها را در iframe هم‌مبدأ باز می‌کند، اما درخواست‌های پس‌زمینه شبیه‌سازی می‌شوند؛ کلید اتصال در شواهد تصویری با مقدار آزمایشی جایگزین شده است. اتصال واقعی به سایت مدرسه، Windows بومی و گوشی فیزیکی در این اجرا آزموده نشده‌اند.

- [سلامت پایگاه در موبایل](docs/settings-health/health.png)
- [همگام‌سازی دسکتاپ در تب](docs/settings-health/desktop/sync.png)
- [مدیریت مدیران در تب](docs/settings-health/admins.png)
- [گزارش مرورگر سایت](docs/settings-health/browser.json) · [گزارش دسکتاپ](docs/settings-health/desktop/browser.json)
- [خلاصهٔ اعتبارسنجی](docs/settings-health/verification.json)

## بازتولید

```sh
python3 scripts/stage-settings-health.py
python3 scripts/stage-settings-health.py --desktop
SITE=.cache/mobile-hubs/site node tests/test-settings-health.mjs
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www HEALTH_DESKTOP=1 node tests/test-settings-health.mjs
node tests/test-settings-health-browser.mjs
HEALTH_DESKTOP=1 node tests/test-settings-health-browser.mjs
SITE=.cache/mobile-hubs/site bash scripts/run-tests.sh
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www bash scripts/run-tests.sh
python3 scripts/release-settings-health.py
python3 tests/test-settings-health-packaging.py
```

وابستگی‌های PHP در `tests/package-lock.json` ثبت‌اند. مرورگر از Playwright، @sparticuz/chromium و @axe-core/playwright در `.cache/browser/node_modules` استفاده می‌کند؛ در sandbox این اجرا `LD_LIBRARY_PATH=$PWD/.cache/browser/lib/lib` نیز لازم بود. builderهای تاریخی را دوباره اجرا نکنید.
