# اصلاحی «بازیابی و رابط» — چهار رفع ایراد در یک بسته

تاریخ: ۱۸ شهریور ۱۴۰۵ · نصب روی آخرین وضعیت منتشرشده (event-sync) ·
هر توزیع دو فایل دارد:
- `includes/header.php`
- `import-photos.php`

## چهار اصلاح

### ۱) تاریخ هدر بدون «مورخ:»
برچسب «مورخ:» از هدر حذف شد؛ فقط **روز و تاریخ** نمایش داده می‌شود (مثلاً
«جمعه ۲۷ شهریور ۱۴۰۵») تا فضای بیشتری برای عنوان صفحه بماند.

### ۲) تب «آپلود تصاویر ZIP» در بازیابی دانش‌آموز — بازیابی شد
ابزار اصلی «ورود گروهی عکس‌ها» در بسته‌های قبلی موجود نبود و پیام
«در نسخهٔ مبنا موجود نیست» نشان داده می‌شد. حالا ابزار اصلی از آرشیو
اصلی (code.zip — commit `15b59c2`) بازیابی، تطبیق و آزمایش شد:
- ZIP حاوی عکس‌هایی که نام هر فایل **کد ملی ۱۰ رقمی** دانش‌آموز است
  (فایل‌های ۹ رقمی با صفر سرِ شماره هم شناسایی می‌شوند)
- هر تصویر مطابق در `uploads/photos/<کد ملی>.jpg` ذخیره و
  `photo_url` دانش‌آموز به‌روز می‌شود — **تغییر واقعی است و به سایت همگام می‌شود**
- اگر ذخیرهٔ فایل ناموفق باشد، **هیچ** نشانی اشتباهی در بانک نوشته نمی‌شود
- گزارش مرحلهٔ دوم: تطبیق‌شده‌ها / بی‌نامعتبر / نامشخص
- دسترسی: همان حق «بازیابی دانش‌آموز» (import_data)

### ۳) در نرم‌افزار دیگر چیزی در پنجرهٔ جدید باز نمی‌شود
در نسخهٔ دسکتاپ یک حفاظت سراسری اضافه شد:
- دکمهٔ «طراحی اختصاصی این کلاس» (ویرایشگر زندهٔ آزمون‌های حضوری) و همهٔ
  لینک‌های `target="_blank"` (چاپ برنامه، سربرگ سوالات، کارت صندلی، چاپ پایه،
  پیش‌نمایش‌ها، صفحهٔ تشخیص) **در همان پنجرهٔ برنامه** باز می‌شوند؛
  برگشت با دکمهٔ back پنل ممکن است.
- فراخوانی‌های `window.open(...)` نیز به همان پنجره هدایت می‌شوند.
- کلیکِ با Meta/Shift/Ctrl یا کلیک وسط هنوز عمل کاربر است و رفتار اصلی
  مرورگر را حفظ می‌کند.
- **وب‌سایت دست‌نخورده است** (پنجرهٔ جدید برای چاپ در سایت کار می‌کند).

### ۴) اعداد فارسی با فونت‌های جایگزین
زنجیرهٔ فونتِ صفحه‌ها به این شکل شد:
`[فونت انتخابی مدرسه] → Vazirmatn → Sahel → Yekan → Tahoma`
اگر فونت انتخابی (مثلاً فونت بارگذاری‌شدهٔ مدرسه) در نسخهٔ دسکتاپ موجود
نباشد یا بارگیری‌اش نشود، متون روی **فونت‌های همراهِ برنامه** می‌نشینند
(اعدادشان فارسی است) و نه روی Tahoma (که اعداد رشته را به‌صورت انگلیسی
نشان می‌داد). اعداد درج‌شده در خود داده‌ها (مثلاً شماره‌هایی که با رقم
انگلیسی ذخیره شده‌اند) به همین دلیل همیشه فارسی دیده می‌شوند.

> یادآوری: اگر اعداد یک بخشِ خاص هنوز انگلیسی بودند، آن بخش ارقام را
> از فونت نمی‌گیرد بلکه خودِ متن انگلیسی است؛ صفحه/فیلد دقیق را بگویید
> تا همان نمایش فارسی (tr_num) در همان صفحه اعمال شود.

## نصب

- **سایت:** محتویات `SITE-FIX-v4.152.0-recovery-ui.zip` را روی پوشهٔ نصب سایت جایگزین کنید.
- **دسکتاپ:** محتویات `SchoolDeskPro-FIX-v2.83.0-recovery-ui.zip` را در همان سطح `SchoolDeskPro` استخراج کنید.
- **به ری‌استارت یا daemon نیاز نیست** — هر دو فایل در هر درخواست اجرا می‌شوند؛
  کافی است یک‌بار صفحه را تازه کنید (F5).
- ترتیب: این بسته پس از بستهٔ `event-sync` نصب می‌شود.

## بازگشت

هر دو فایل را از بسته‌های قبلی (پیش از این بسته) بازگردانید و صفحه را تازه کنید.

## شواهد

- ۲۸ بررسی دسکتاپ + ۲۲ بررسی سایت برای همین چهار اصلاح
  (`tests/test-desktop-fixes.mjs`) — شامل آپلود واقعی ZIP در هارنس و
  آزمایش منطق حفاظتِ پنجره با DOM شبیه‌سازی‌شده
- ۳۴ + ۴۵ بررسی صف ربات (با endpoint واقعی) · ۴۷ بررسی بهینه‌سازی
- **۳۱/۳۱ + ۳۱/۳۱** سوئیت رگرسیون کامل دسکتاپ و سایت
- بررسی‌های بسته‌بندی (`tests/test-recovery-ui-packaging.py`)
- ۱۵۰ بایگانی قبلی بایت‌به‌بایت دست‌نخورده

## حدومرزها

- تست روی PHP 8.3 (WASM) + SQLite با شبکهٔ شبیه‌سازی‌شده؛ آپلود ZIP
  سر‌به‌پا در هارنس انجام شد. **اندازه‌گیری زنده روی هاست/ویندوز
  (WebView) انجام نشده** — رفتار «پنجرهٔ جدید» در مرورگرهای خیلی قدیمی
  ممکن است متفاوت باشد؛ اگر دکمهٔ خاصی باز هم پنجره باز کرد، آدرسش را
  بگویید.
- ابزار عکس‌ها فقط عکس را به پروندهٔ دانش‌آموزی که **کد ملی‌اش موجود و
  active است** وصل می‌کند؛ دانش‌آموز حذف‌شده یا کد ملی ناقص، لمس نمی‌شود.

## فایل‌ها و امضا

- `SchoolDeskPro-FIX-v2.83.0-recovery-ui.zip` (دسکتاپ، ۲ فایل)
- `SITE-FIX-v4.152.0-recovery-ui.zip` (سایت، ۲ فایل)
- امضا: `RECOVERY-UI-SHA256SUMS.txt`
- شواهد: `docs/recovery-ui/packages.json` و `docs/recovery-ui/verification.json`
