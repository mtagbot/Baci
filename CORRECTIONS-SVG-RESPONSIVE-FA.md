# رابط SVG و چیدمان واکنش‌گرا — بستهٔ اصلاحی، ۲۰۲۶/۰۹/۱۷

## دانلود از مخزن

- [اصلاحی سایت — SITE-FIX-v4.152.0-svg-responsive.zip](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-svg-responsive.zip)
- [اصلاحی دسکتاپ — SchoolDeskPro-FIX-v2.83.0-svg-responsive.zip](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-svg-responsive.zip)
- [SHA256](SVG-RESPONSIVE-SHA256SUMS.txt) · [فهرست دقیق ۵۳ فایل](scripts/ui-patch-files.json)

**فقط دو اصلاحی جدید ساخته شده‌اند.** هر بسته ۵۳ فایل تغییرکرده/جدید برنامه دارد، حدود ۳۴۴ کیبی‌بایت؛ نسخهٔ کامل نیست. چهار ZIP منتشرشدهٔ قبلی، از جمله هر دو `Release_V1.0`، با همان بایت‌ها و SHA256 قبلی باقی مانده‌اند. شمارهٔ داخلی سایت 4.152.0 و دسکتاپ 2.83.0 تغییر نکرده است.

### نصب روی نسخهٔ موجود

پیش‌نیاز: آخرین نصب با اصلاحات قبلی تا `db5c791`، از جمله Release_V1.0 منتشرشده. این بسته برای نسخه‌های قدیمی‌ترِ بدون اصلاحات قبلی، ارتقای مستقل نیست.

1. از برنامه، دیتابیس و `uploads` پشتیبان مستقل بگیرید؛ برنامهٔ دسکتاپ را کاملاً ببندید.
2. سایت: **محتویات** `site-update-v4.152.0` را در ریشهٔ سایت موجود ادغام/جایگزین کنید.
3. دسکتاپ: پوشهٔ `SchoolDeskPro/www` بسته را با `www` موجود ادغام کنید؛ پوشهٔ تو‌در‌تو نسازید.
4. همهٔ فایل‌ها، به‌ویژه `includes/school_ui_theme.php` و سه دارایی `school-*` را منتقل کنید. OPcache را در صورت نیاز بازنشانی و مرورگر را با Ctrl+F5 نوسازی کنید.
5. ورود، بازشدن فهرست موبایل، صفحهٔ دانش‌آموزان و یک چاپ آزمایشی را در محیط خود بررسی کنید.

دیتابیس، تنظیم اتصال، اطلاعات ورود، فایل اجرایی ویندوز، بارگذاری‌ها، نشست‌ها، اسکنر بازگشت و نصب‌کننده در بسته نیستند. این تغییر رابط، مهاجرت دیتابیس ندارد. برای بازگشت، همان ۵۳ مسیر را از پشتیبان قبل از نصب برگردانید و فایل‌های تازهٔ `school-*` را پس از بازگردانی هدر/فوتر حذف کنید؛ اطلاعات مدرسه را پاک نکنید.

## تغییرات رابط

- خانوادهٔ محلی **۶۱ SVG** با هندسهٔ ثابت و موضوع مدرسه، آزمون، محتوا، گزارش، امنیت و ارتباطات؛ بدون وابستگی آیکون به اینترنت یا فونت ایموجی.
- ۱۸۶ جایگزینی ایستای HTML در ۴۱ فایل، به‌علاوهٔ تبدیل کنترل‌های تولیدشده با جاوااسکریپت. فقط هندسهٔ ثابت برنامه به SVG تبدیل می‌شود؛ SVG کاربر وارد این مسیر نمی‌شود.
- ایموجی‌های تزئینیِ گزینه‌های بومی و پیام‌های `alert/confirm/prompt` به متن خوانا تبدیل می‌شوند، چون این کنترل‌ها SVG نمی‌پذیرند. مقدار واقعی گزینه، نتیجهٔ تأیید و شکست سطر پیام حفظ می‌شود.
- متن آزادِ ناشناخته، سلول‌های دادهٔ متنی، ویرایشگر سؤال، کد، ورودی‌ها و پیام ذخیره‌شدهٔ ربات از تبدیل تزئینی محافظت می‌شوند. این سازوکار جایگزین پاک‌سازی امنیتی محتوای کاربر نیست.
- نمای روشن، کارت‌ها و فرم‌های هماهنگ، رنگ‌های خواناتر و فوتر با هویت مدرسه و پیوندهای کاربردی. چهار رنگ ذخیره‌شده حفظ می‌شوند؛ رنگ نمایشی دکمه‌ها/متن برای خوانایی مشتق می‌شود، بدون تغییر رنگ خروجی چاپ/فایل.
- موبایل: منوی کشویی، دکمهٔ بستن، مدیریت فوکوس و Escape، کنترل‌های لمسی بزرگ‌تر، ستون‌بندی و نوار عملیات مناسب عرض کم، فوتر چندبخشی.
- تبلت و دسکتاپ: تنظیم تعداد ستون‌ها، نوار کناری و تراکم متفاوت؛ جدول عریض و کاغذ چاپ به‌صورت محلی پیمایش می‌شوند، نه با خارج‌کردن کل صفحه از عرض دستگاه.
- اصلاح ارتفاع بزرگ هدر و بریده‌شدن محتوای اصلی ناشی از قواعد قدیمیِ پنجرهٔ ثابت. نمایش آیکون حساب کاربری روی موبایل نیز حفظ شده است.
- تب ورود با نقش و برچسب صحیح، کلیدهای جهت راست‌به‌چپ/Home/End، پیوند پرش به محتوا، برچسب ورودی‌های رنگ و انتخاب ردیف، فوکوس واضح و رعایت کاهش حرکت/رنگ‌های اجباری.
- بزرگ‌نمایی کاربر در اسکنر آزاد است. هندسهٔ کاغذ و کارت با قواعد مخصوص چاپ از بازطراحی صفحه جدا شده است.

## شواهد آزمون و حدود آن

[نتایج قابل بررسی و فهرست مسیرها](docs/ui-svg-responsive/audit.json) · [تصویر موبایل](docs/ui-svg-responsive/mobile.png) · [تصویر دسکتاپ](docs/ui-svg-responsive/desktop.png)

تصاویر و داده‌های آزمون ساختگی‌اند و از دیتابیس مدرسهٔ واقعی گرفته نشده‌اند.

| بررسی | نتیجه |
|---|---|
| فهرست ورودی‌ها | ۹۹ ورودی ریشه/API؛ ۷۵ درخواست نقش/حالت |
| HTML کامل بدون خطای fatal | ۷۳ حالت از ۶۵ مسیر؛ دو GET ناقص گزارش گروهی صفحه ندارند و POST همان گزارش‌ها پوشش داده شده است |
| Chromium در عرض‌های ۳۲۰، ۳۹۰، ۷۶۸، ۱۰۲۴ و ۱۴۴۰ | ۳۶۵ حالت؛ صفر خروج سند از عرض، صفر هم‌پوشانی فوتر، صفر بریدگی پوستهٔ اصلی، صفر هدر بیش‌ازحد بلند و صفر خطای ثبت‌شدهٔ جاوااسکریپت |
| Axe در پنج نمونهٔ نقش/صفحه | ورود مدیر، داشبورد مدیر، تنظیمات، دانش‌آموزان و ورود عمومی؛ صفر تخلف گزارش‌شده با برچسب‌های A/AA و آزمون‌های موجود AAA |
| تعامل و حفظ محتوا | ۱۲ بررسی موفق: منوی موبایل، فوکوس/Tab/Escape، تب ورود، جدول، متن/مقدار فرم، گزینهٔ پویا، تأیید بومی، اجرای تکراری، غیرفعال‌بودن observerها و منوی پایه بدون JavaScript |
| مجموعه‌های بازگشت قبلی | هر ۳۰ سوئیت روی سایت و هر ۳۰ سوئیت روی کد وب دسکتاپ موفق |
| چاپ واقعی Chromium | ۲۷ حالت پیش‌نمایش/چاپ/PDF کارت؛ کارت سفارشی، خواندن QR، نام بلند، ذخیرهٔ انتخاب و بازگردانی طرح قدیمی موفق |
| قلم/ورود/لیست دبیر | سه قلم همراه و TTF اختصاصی، کپچای تطبیقی، دو سرستون و دقیقاً دو صفحهٔ A4 در اندازهٔ عادی و حداکثر کنترل‌ها موفق |
| لیست مدرسه | ۲۰ نمونهٔ A4/A3، نام ترکیبی/تفکیکی و تراکم متفاوت در docx-preview/Chromium؛ یک صفحه و بدون خروج جدول |
| بسته‌بندی | ۴ آزمون: payload دقیق، checksum، نبود داده/اجرایی و دست‌نخورده‌بودن ZIPهای قبلی |

بیشترین زمان ثبت‌شدهٔ شروع **لایهٔ جدید** در اجرای نهایی ۸۲ میلی‌ثانیه بود؛ این عدد زمان بارگذاری کامل صفحه، سرعت سرور یا تضمین عملکرد گوشی ضعیف نیست. اسکریپت جدید ES5 است، چارچوب تازه یا polling اضافه نمی‌کند، تغییرات DOM را دسته‌بندی می‌کند و صف پردازش محدود دارد. برنامهٔ قدیمی همچنان در بعضی قسمت‌ها به JavaScript جدید، `fetch` و قابلیت‌های دوربین وابسته است؛ آزمون حذف چند قابلیت در Chromium، تأیید IE11 یا دستگاه قدیمی واقعی محسوب نمی‌شود.

**این گزارش گواهی WCAG AAA نیست.** Axe نمونه‌ای اجرا شده است، نه روی تمام حالت‌های تعاملی. صفحه‌خوان واقعی، مرورگرهای قدیمی واقعی، دوربین گوشی، ویندوز بومی، Microsoft Word، GD/FreeType بومی و چاپگر فیزیکی در این مرحله تأیید نشده‌اند. آزمون Word با docx-preview جایگزین بازکردن سند در Word نیست؛ قلم جایگزین آزمایش، خانوادهٔ قلم سند تولیدی را تغییر نمی‌دهد.

مرور صفحات با خروجی واقعی PHP در SQLite یک‌بارمصرف و سپس Chromium انجام شده است؛ درخواست‌های PHP پس‌زمینه در آزمون چیدمان پاسخ شبیه‌سازی‌شده می‌گیرند و میزبان‌های خارجی مسدودند. بنابراین این نتیجه، آزمون زندهٔ همهٔ کلیک‌ها، سرویس‌های پیام‌رسان یا شبکهٔ مدرسه نیست. APIها، دانلودها، خروج و عملیات نگهداریِ تغییردهندهٔ داده به‌عنوان صفحهٔ عادی crawl نشده‌اند. فرم امن GETِ `migration-updater.php` بررسی شده؛ دو اسکریپت مهاجرتی که روی GET تغییر داده می‌دهند عمداً اجرا نشده‌اند. نصب‌کننده و اسکنر قدیمیِ بازگشت دست‌نخورده‌اند.

### مسیرهای قدیمیِ ناموجود — هنوز حل نشده

در نسخهٔ کامل مبنا، این سه فایل وجود ندارند، ولی رابط به آن‌ها ارجاع دارد:

- `import.php`: داشبورد و مرکز مدیریت کارنامه‌ها.
- `import-photos.php`: ابزار بازیابی دانش‌آموزان.
- `grade-entry-management.php`: مرکز مدیریت کارنامه‌ها.

این لینک‌ها مخفی یا با موفقیت ساختگی جایگزین نشده‌اند؛ بستهٔ ظاهری حاضر آن قابلیت‌های مفقود را پیاده‌سازی نمی‌کند. بنابراین عبارت «همهٔ لینک‌ها و قابلیت‌های سایت سالم‌اند» دربارهٔ این تحویل درست نیست. پیش از ادعای نسخهٔ کامل بی‌نقص باید این مسیرها جداگانه بازیابی/اصلاح و آزموده شوند.

## بازتولید

ابتدا وابستگی‌های `tests` و برای آزمون مرورگر، `playwright`، `@sparticuz/chromium`، `@axe-core/playwright` و `acorn` را در `.cache/browser` نصب کنید. کتابخانه‌های سیستم لازم برای Chromium نیز باید در محیط موجود باشند.

```bash
python3 scripts/stage-ui-patch.py
SITE=.cache/ui-audit/current bash scripts/run-tests.sh
SITE=.cache/ui-audit/current node tests/test-ui-page-catalog.mjs
SITE=.cache/ui-audit/current node tests/test-ui-responsive-browser.mjs
SITE=.cache/ui-audit/current node tests/test-ui-interactions-browser.mjs
python3 scripts/release-svg-responsive.py
python3 tests/test-svg-responsive-packaging.py
```

اسکریپت stage فقط نسخهٔ کامل **موجود** را در cache استخراج و وصله را روی آن اعمال می‌کند؛ نسخهٔ کامل نمی‌سازد. سازندهٔ اصلاحی نیز فقط دو ZIP جدید بالا را تولید می‌کند و SHA256 چهار آرشیو قبلی را پیش و پس از اجرا کنترل می‌کند. آزمون چیدمان در صورت خروج از عرض، بریدگی پوسته، هدر نامتعارف، هم‌پوشانی فوتر، خطای JS یا تخلف Axe نمونه‌ای، با خطا پایان می‌یابد.
