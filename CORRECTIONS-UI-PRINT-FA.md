# اصلاحات ورود، قلم، رنگ و چاپ — بدون تغییر نسخهٔ داخلی

## بستهٔ اصلاحی برای نصب موجود

- [اصلاحی سایت — SITE-FIX-v4.152.0-ui-print.zip](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-ui-print.zip)
- [اصلاحی دسکتاپ — SchoolDeskPro-FIX-v2.83.0-ui-print.zip](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-ui-print.zip)
- [SHA256 اصلاحی‌ها](UI-PRINT-SHA256SUMS.txt)

این دو بسته فقط **۲۴ فایل برنامه** دارند و برای آخرین نصب 4.152.0 / 2.83.0 با اصلاحات قبلی، از جمله Release_V1.0، هستند؛ نصب مستقل نیستند. شمارهٔ داخلی تغییر نکرده است. فایل تنظیم اتصال، کلید خصوصی، دیتابیس، تصاویر و فونت‌های بارگذاری‌شده، نشست‌ها و اسکنر قدیمی را جایگزین نمی‌کنند. نگهبان نصب نسخهٔ کامل نیز حفظ می‌شود.

1. از دیتابیس، پوشهٔ برنامه و `uploads` پشتیبان مستقل بگیرید. دسکتاپ را کاملاً ببندید.
2. سایت: **محتویات** `site-update-v4.152.0` را در ریشهٔ همان سایت موجود ادغام/جایگزین کنید.
3. دسکتاپ: `SchoolDeskPro/www` بسته را با `www` برنامهٔ موجود ادغام کنید؛ پوشهٔ جدید تو‌در‌تو نسازید.
4. PHP/برنامه را مجدداً اجرا کنید؛ در صورت استفاده از OPcache آن را از پنل هاست بازنشانی کنید. مرورگر را با Ctrl+F5 نوسازی کنید.
5. بخش سفارشی‌سازی را بررسی کنید. دو رنگ جدید مقدار پیش‌فرض دارند؛ ذخیرهٔ تنظیمات، چهار رنگ و قلم مشترک را نگه می‌دارد. جدول کوچک شمارش خطای ورود در صورت نیاز خودکار ساخته می‌شود؛ هیچ داده‌ای حذف نمی‌شود.

## اصلاحات ده‌گانه

1. **ورود:** اولین تلاشِ نشست/نقش جدید بدون نمایش و بدون الزام کپچا است. خطای نام کاربری/رمز، از تلاش دوم کپچا را در HTML و سمت سرور فعال می‌کند؛ عوض‌کردن نام کاربری آن را خاموش نمی‌کند. پاسخ خالی یا درخواست مستقیم بدون سؤالِ تولیدشده پذیرفته نمی‌شود. محدودیت ۶ خطا در نشست و سقف مستقل ۳۰ خطا در ده دقیقه برای IP، جدا از کپچا اعمال می‌شوند.
2. **قلم کارنامه:** پیش‌نمایش، PDF واقعی و انتخاب قلم تصویر از یک مسیر محلی استفاده می‌کنند. PDF، همان TTF را درون فایل قرار می‌دهد؛ Excel نام قلم انتخابی را اعلام می‌کند. نمودار مرورگر نیز قلم تنظیم‌شده را می‌گیرد.
3. **QR مربعی:** قاب QR روی کارت از ۴۹ به ۵۰٫۴ و پشت کارت از ۵۰ به ۵۲ میلی‌متر افزایش یافته؛ فضای اضافی کمتر شده، ولی حاشیهٔ سفید استاندارد چهارماژولی حذف نشده است.
4. **سرستون دبیر:** ادغام سلول‌های «فعالیت درسی» اصلاح شده تا «نام» و «نام خانوادگی» هر دو در ستون خود دیده شوند.
5. **ناحیهٔ چاپ:** در خروجی HTML/PDF دبیر و Word مدرسه حدود ۶ میلی‌متر حاشیهٔ امن لحاظ شده است؛ هیچ دانش‌آموزی حذف نمی‌شود. فهرست مدرسه همچنان یک صفحه است، با هر دو حالت نام ترکیبی/تفکیکی و A4/A3 افقی.
6. **صفحهٔ سوم سفید:** صفحه‌های دبیر اندازهٔ فیزیکی ثابت دارند و محتوا پس از بارگذاری قلم و تغییر اسلایدرها متناسب جا داده می‌شود؛ شکست اضافهٔ صفحهٔ آخر حذف شده است.
7. **قلم رابط:** عنوان‌ها، فرم‌ها، جدول‌ها و صفحه‌های مستقل، قلم سفارشی‌سازی را می‌گیرند. خانوادهٔ آیکون‌ها و فرمول‌ها، قلم صریح سؤال در ویرایشگر و قالب ثابت تیتر فهرست‌ها حفظ می‌شوند. چاپ منتظر آماده‌شدن قلم می‌ماند.
8. **چهار رنگ:** اصلی، تأکیدی، موفقیت و خطا در تنظیمات و عناصر مرتبط رابط و نمودارها هماهنگ شده‌اند؛ متن دکمه‌ها با توجه به روشنایی رنگ انتخاب می‌شود.
9. **کلاس مستثنا:** ردیف قدیمیِ عضو مستثنا فقط وقتی آزمون مستقل معتبر دارد، از جدول مدیر/معاون پنهان می‌شود؛ آزمون مستقل باقی است و داده‌ای حذف نمی‌شود.
10. **پیام ویرایشگر:** «آزمون مشترک پایه — نام کلاس‌های مشارکت‌کننده» یا «آزمون مستقل این کلاس — نام کلاس» نمایش داده می‌شود؛ کلاس مستثنا در فهرست مشترک نمی‌آید.

**Word دبیر تغییر نکرده است.** حاشیهٔ جدید مدرسه، درخواست تازهٔ ایمنی چاپ را با اولویت قبلیِ تک‌صفحه بودن ترکیب می‌کند. هدف نام‌ها ۸ پوینت است؛ اگر بودجهٔ ارتفاع/عرض کافی نباشد، طبق اولویت `one_page_first` کمی کوچک‌تر می‌شوند؛ صفحهٔ ادامه اضافه نمی‌شود. در نمونهٔ معمول A4 با حاشیهٔ تازه، اندازه می‌تواند ۷٫۵ پوینت باشد.

## قلم و چاپ: شرایط لازم

- برای یکسانی قابل‌حملِ PDF و تصویر، از TTF محلی مانند وزیرمتن/ساحل/یکان یا TTF مجازِ خودتان استفاده کنید. فونت بارگذاری‌شده باید مجوز استفاده و جاسازی لازم را داشته باشد.
- **Tahoma سیستمی** در همهٔ دستگاه‌ها موجود نیست. WOFF/WOFF2 مستقیماً در GD/TCPDF قابل استفاده نیستند؛ PDF آن‌ها از مسیر چاپ مرورگر ارائه می‌شود. برای تصویر سرور، TTF همان قلم لازم است. قلم متفاوت بی‌صدا جایگزین تصویر نمی‌شود.
- Excel قلم را جاسازی نمی‌کند؛ برای ظاهر یکسان باید همان قلم روی دستگاه بازکننده نصب باشد. فایل TTF اختصاصی برای Excel با نام داخلی واقعی آن معرفی می‌شود، نه نام مستعار CSS.
- تصویر سرور به GD **با FreeType واقعی** وابسته است. PDF به پوشهٔ خصوصی قابل‌نوشتن `backups/font-cache` نیاز دارد؛ این پوشه نباید از وب قابل دانلود باشد.
- برای دبیر A4 عمودی، و برای مدرسه A4/A3 افقی انتخاب کنید. حاشیهٔ امن ۶ میلی‌متر برای بسیاری از چاپگرها مناسب است، نه تضمین همهٔ مدل‌ها. اگر چاپگر حاشیهٔ سخت‌افزاری بزرگ‌تری دارد، گزینهٔ Fit to printable area را فعال کنید.

## آزمون‌ها و حدود اعتبار

۳۰ سوئیت بازگشت برای سایت و دسکتاپ؛ ۳۱ بررسی اختصاصی اصلاحات در هرکدام، ۵۸ بررسی نصب تازهٔ SQLite، و ۳ آزمون بسته‌بندی اصلاحی. اصلاحی سایت روی ZIP کامل قبلی نیز اعمال و بررسی‌های اختصاصی دوباره اجرا شدند.

- آزمون‌های PHP واقعیِ ورود اول/خطا/کپچای اجباری/ورود موفق/نشست جدید و محدودیت پایدار IP.
- PDF واقعی سه قلم همراه و اعلان قلم Excel، قلم اختصاصی TTF، رنگ‌ها، نبود ردیف تکراری در مدیر و معاون و نام کلاس‌های پیام ویرایشگر.
- Chromium: قلم‌های واقعاً بارگذاری‌شده، کپچای مخفی/فعال، QR بزرگ‌تر، دو سرستون و **دقیقاً دو صفحهٔ A4** در اندازهٔ عادی و حداکثر اسلایدرهای دبیر.
- ۲۰ نمونهٔ فهرست مدرسه در `docx-preview` و Chromium: یک صفحه، حاشیهٔ امن و مرکزچینی، هر دو کاغذ و چیدمان، از جمله ۳۰ و ۶۰ ردیف. این آزمون **Microsoft Word نیست**؛ در محیط آزمایش به‌جای 2 Titr از B Titr استفاده شده و قلم سند تولیدی عوض نشده است.
- آزمون کارت سفارشی و ۲۷ حالت پیش‌نمایش/چاپ/PDF کارت‌ها، از جمله خواندن QR و نبود بریدگی نام‌های بلند.
- PHP این محیط WebAssembly است؛ GD آن با وجود گزارش FreeType، رسم TTF را پشتیبانی نمی‌کند. انتخاب دقیق فایل و خطای روشنِ عدم پشتیبانی آزمایش شده/قابل بررسی است، اما **رندر نهایی GD، Word بومی، اجرای ویندوز و چاپگر فیزیکی در این محیط تأیید نشده‌اند**.

فرمان‌های بازتولید در `scripts/release-ui-print.py`، `tests/test-quality-improvements.mjs`، `tests/test-quality-browser.mjs` و `tests/test-ui-print-packaging.py` قرار دارند. وابستگی‌ها و خروجی‌های آزمایشی داخل ZIP نیستند.
