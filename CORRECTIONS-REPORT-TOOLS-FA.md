# بازیابی واقعی ایمپورت Wizard و مدیریت ثبت نمره

## علت پیام «در نسخهٔ مبنا موجود نیست»

دو فایل `import.php` و `grade-entry-management.php` در سورس استخراج‌شده‌ای که مبنای بستهٔ کامل قبلی بود، جا افتاده بودند. در اصلاحی student-workflow برای مسیرهای گم‌شده صفحهٔ جایگزینِ صریح قرار گرفت؛ اصلاحی mobile-hubs فقط اندازهٔ قاب و تب‌ها را اصلاح کرد و این دو ابزار را بازسازی نکرد. بنابراین پیام مربوط به نبود فایل ابزار بود، نه خرابی اطلاعات مدرسه یا اندازهٔ صفحه.

در این بررسی، **نسخهٔ اصلی هر دو صفحه داخل `code.zip` مخزن اصلی پیدا شد**. رابط و رفتار اصلی بازیابی و مسیر اجرای داده با اعتبارسنجی و تراکنش اصلاح شد؛ این کار فقط حذف پیام یا تغییر پیوند تب نیست.

منبع بازیابی: [code.zip در commit اصلی](https://github.com/Maxess/mtagbot/blob/15b59c2182c5d83e753b3adca3b0ca0acc6a6530/code.zip). هش آرشیو و دو فایل اصلی در [گزارش منشأ و آزمون](docs/report-tools/verification.json) ثبت شده است. نسخهٔ منتشرشده عمداً کپی بایت‌به‌بایت کد قدیمی نیست؛ اشکال‌های ثبت داده و تکمیل نمایشی اصلاح شده‌اند.

## دانلود و نصب

**فقط اصلاحی؛ هر بسته ۷ فایل و حدود ۱۷ کیلوبایت است.** بستهٔ کامل، exe و شمارهٔ داخلی نسخه تغییر نکرده‌اند.

- [اصلاحی سایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-report-tools.zip)
- [اصلاحی دسکتاپ](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-report-tools.zip)
- [SHA256](REPORT-TOOLS-SHA256SUMS.txt) · [فهرست ۷ فایل](scripts/report-tools-files.json)

این بسته **پس از mobile-hubs** نصب شود:

`Release_V1.0 → svg-responsive → student-workflow → preview-navigation → fast-ui → mobile-hubs → report-tools`

۱. از نصب و پایگاه‌داده پشتیبان بگیرید.
۲. سایت: محتویات `site-update-v4.152.0` را با ریشهٔ نصب ادغام و فایل‌های هم‌نام را جایگزین کنید. پوشهٔ نصب را حذف نکنید.
۳. دسکتاپ: برنامه را کاملاً ببندید؛ محتویات `SchoolDeskPro/www` را با `www` موجود ادغام کنید و برنامه را دوباره اجرا کنید.
۴. در صورت استفاده از OPcache بدون بررسی تغییر فایل، کش PHP را بازنشانی کنید. سپس صفحهٔ مدیریت کارنامه‌ها را تازه کنید.

هیچ پایگاه‌داده، تنظیمات اتصال، تصویر، قلم یا دادهٔ مدرسه در ZIP نیست. فایل‌های ZIP قبلی با مقایسهٔ هش کنترل شدند و تغییر نکرده‌اند. `import-photos.php` خارج از دامنهٔ این اصلاحی است و بازنویسی نشده است.

## ایمپورت Wizard

مسیر: **مدیریت کارنامه‌ها ← ایمپورت Wizard**

- بارگذاری فایل، پیش‌نمایش، نمایش ابهام‌ها، انتخاب سال مقصد، تأیید نهایی، نتیجه و سوابق نشست‌ها برگشته‌اند.
- فایل نمونهٔ قابل دانلود، فقط سرستون‌های لازم دارد؛ هیچ اطلاعات دانش‌آموز واقعی یا نمونه‌ای به مدرسه اضافه نمی‌کند.
- CSV ساختاریافتهٔ UTF-8، قالب بلوکی قدیمی Report.csv و XLSX تک‌برگه پشتیبانی می‌شوند. XLS قدیمی باید در Excel به CSV یا XLSX ذخیره شود.
- XLSX به افزونه‌های PHP ZIP و SimpleXML نیاز دارد. در نبود آن‌ها پیام راهنما نشان داده می‌شود؛ CSV همچنان قابل استفاده است. فرمول‌ها را به مقدار تبدیل کنید و فقط یک برگهٔ داده نگه دارید. کد ملی را در Excel به صورت **Text** نگه دارید تا صفر ابتدایی حذف نشود.
- حداکثر فایل ۱۰ مگابایت و ۵۰۰۰ ردیف نمره است؛ فایل بزرگ‌تر را تقسیم کنید. سقف حجم بازشدهٔ XLSX نیز ۴۰ مگابایت است.
- دانش‌آموز باید از قبل موجود باشد. تطبیق با کد ملی یا تطبیق یکتای نام و کلاس انجام می‌شود. دانش‌آموز مجهول یا مبهم، نمرهٔ نامعتبر، درس تکراری در همان دوره و فایل ناقص، مانع ثبت می‌شوند؛ ردیف‌ها بی‌صدا نادیده گرفته نمی‌شوند.
- فایل را پیش از تأیید اصلاح و دوباره بارگذاری کنید. تا تأیید نهایی، نمره‌ای ثبت نمی‌شود. پردازش نهایی دوباره دانش‌آموز، سال، نمره‌ها و تعارض‌ها را بررسی می‌کند.
- فقط درس‌های داخل فایل افزوده یا جایگزین می‌شوند؛ سایر درس‌ها، مشخصات دانش‌آموز و کلاس فعلی او تغییر نمی‌کنند. کارنامهٔ قفل‌شده بازنویسی نمی‌شود.
- عدد ۲۱ غیبت است و از معدل کنار گذاشته می‌شود؛ انضباط نیز طبق تابع محاسبهٔ موجود سامانه جدا محاسبه می‌شود. صفر با خانهٔ خالی فرق دارد.
- ثبت در یک تراکنش است؛ خطا باعث برگشت کل تغییرات همان درخواست می‌شود. درخواست تکراری همان نشست دوباره نمره درج نمی‌کند. تعداد نتیجه، شمار ردیف‌های واقعاً ثبت‌شده است.
- نشست و پیش‌نمایش به مدیر بارگذارنده محدودند. فایل خام در پوشهٔ عمومی uploads نگه‌داری نمی‌شود؛ دادهٔ پیش‌نمایش کامل در جدول موجود نشست‌ها ذخیره می‌شود.
- نشست‌های قدیمی که فقط ۵۰ ردیف اولشان ذخیره شده بود، ممکن است نیازمند بارگذاری مجدد فایل باشند؛ با دادهٔ ناقص، تکمیل موفق نمایش داده نمی‌شود.

**دسترسی:** مجوز اصلی ابزار یعنی `import_data` حفظ شده است. کاربری که فقط `manage_reports` دارد ممکن است تب را ببیند ولی اجازهٔ ایمپورت نداشته باشد؛ در این حالت مدیر باید مجوز لازم را اختصاص دهد، نه اینکه بررسی دسترسی حذف شود.

### مسیر API و صف قدیمی

در کد اصلی، بخشی از API و worker صف فقط وضعیت را «تکمیل‌شده» می‌کردند، بدون ثبت واقعی نمره. برای جلوگیری از بازگشت همین خطا:

- `preview-execute` اکنون همان تراکنش واقعی Wizard را اجرا می‌کند؛ POST، مالکیت نشست و مجوز بررسی می‌شوند. درخواست مبتنی بر نشست، CSRF لازم دارد؛ توکن معتبر مدیر دارای مجوز نیز پشتیبانی می‌شود.
- فهرست نشست‌ها و پیش‌نمایش API به مالک محدود شده‌اند.
- `queue-process` و `resolve-student-edit` قدیمی پیام خطای روشن می‌دهند، نه موفقیت ساختگی. اصلاح اطلاعات باید با بارگذاری دوبارهٔ فایل انجام شود.
- worker شبیه‌سازی‌شدهٔ قدیمی دیگر هیچ وضعیت یا نمره‌ای را تغییر نمی‌دهد؛ اجرای CLI آن با پیام توضیحی و کد خروج ۱ پایان می‌یابد. اگر برای این worker یک cron تنظیم کرده‌اید، آن کار زمان‌بندی‌شده را غیرفعال و فایل را از Wizard تأیید کنید. صف پس‌زمینهٔ واقعی در این اصلاحی ادعا نمی‌شود.

## مدیریت ثبت نمره

مسیر: **مدیریت کارنامه‌ها ← مدیریت ثبت نمره**

- فرم تعریف قانون برای سال، ماه، پایه، کلاس، درس و دبیر، همراه جدول قوانین و حذف قانون برگشته است.
- قوانین در همان جدول `grade_entry_permissions` ذخیره می‌شوند؛ قوانین قبلی پاک نمی‌شوند.
- مجوز `manage_reports` و CSRF لازم‌اند؛ سال و دبیر نامعتبر پذیرفته نمی‌شوند.
- وابستگی جاافتادهٔ تابع تشخیص پایه اصلاح شد تا بررسی قانون در POST پنل دبیر خطای تابع ناموجود ندهد.
- **منطق قبلی حفظ شده:** اگر در هر قانون منطبق، اجازهٔ ثبت یا ویرایش خاموش باشد، پنل دبیر هر دو عملیات را مسدود می‌کند. قانون مجاز، ممنوعیت قبلی را خنثی نمی‌کند؛ برای بازکردن دسترسی، قانون مسدودکننده را حذف کنید. این توضیح در خود صفحه نیز آمده است.

## آزمون‌ها

- **۱۲۸ بررسی واقعی PHP برای سایت و ۱۲۸ برای دسکتاپ**: بارگذاری multipart واقعی، CSV و XLSX، قالب بلوکی، پیش‌نمایش، ثبت واقعی، ۶۰ ردیف به‌جای محدودشدن به ۵۰، ارقام فارسی و اعشار، غیبت و انضباط، سال مقصد، حفظ سایر درس‌ها و مشخصات، مالکیت نشست، CSRF، درخواست تکراری، فایل مبهم و rollback با خطای تزریق‌شدهٔ پایگاه‌داده.
- ذخیره و حذف قانون و اثر آن با POST واقعی پنل دبیر بررسی شد: مسدودسازی مانع ثبت شد و پس از حذف قانون، ثبت انجام شد. API واقعی و احراز هویت Bearer نیز آزموده شدند.
- **۲۰ چیدمان واقعی iframe در هر توزیع، ۷۴ بررسی مرورگر برای هرکدام**: بارگذاری، پیش‌نمایش، نتیجه و قوانین در عرض‌های ۳۲۰، ۳۹۰، ۷۶۸، ۱۰۲۴ و ۱۴۴۰؛ بدون فرم بریده‌شده یا بیرون‌زدگی افقی. نمونه‌برداری Axe از چهار حالت در عرض ۳۹۰ خطایی گزارش نکرد.
- بازآزمایی پنج مرکز مدیریت: **۷۱۵ بررسی و ۸۵ چیدمان تب**، به‌علاوه نماهای داخلی و رفتارهای قبلی. اکنون محتوای واقعی ۱۶ تب در دسترس است؛ فقط تب تصاویر ZIP همچنان پاسخ نبود ماژول دارد.
- **هر ۳۱ مجموعه‌آزمون قبلی در هر دو توزیع موفق‌اند**؛ شامل فونت، فوتر، چاپ، کارت، آزمون کلاسی، اسکنر و اتصال در محدودهٔ harness موجود.
- پنج آزمون بسته‌بندی جدید، پنج آزمون mobile-hubs و پنج آزمون fast-ui موفق بودند؛ فایل‌های دقیق، هش و ثابت‌ماندن آرشیوهای قدیمی کنترل شد.

اجرای PHP با پایگاه‌دادهٔ SQLite آزمایشی و مرورگر Chromium انجام شده است. مرورگر از HTML واقعی خروجی PHP در iframe هم‌مبدأ استفاده می‌کند، ولی APIهای پس‌زمینه در تست چیدمان شبیه‌سازی می‌شوند. این نتیجه تأیید اجرای بومی Windows، MySQL واقعی، تلفن فیزیکی یا دادهٔ مدرسهٔ شما نیست؛ نخست با یک فایل کوچک و پشتیبان داده آزمایش کنید.

### شواهد

- [بارگذاری](docs/report-tools/upload.png) · [پیش‌نمایش](docs/report-tools/preview.png) · [نتیجه](docs/report-tools/result.png) · [قوانین](docs/report-tools/rules.png)
- [گزارش مرورگر سایت](docs/report-tools/browser.json) · [گزارش مرورگر توزیع دسکتاپ](docs/report-tools/desktop/browser.json)
- [بازآزمایی تب‌ها](docs/report-tools/hubs/layouts.json) · [منشأ بازیابی و خلاصهٔ تأیید](docs/report-tools/verification.json)

## بازتولید

```sh
python3 scripts/stage-report-tools.py
python3 scripts/stage-report-tools.py --desktop
SITE=.cache/mobile-hubs/site node tests/test-report-tools.mjs
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www REPORT_TOOLS_DESKTOP=1 node tests/test-report-tools.mjs
SITE=.cache/mobile-hubs/site node tests/test-mobile-hubs-fixtures.mjs
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www HUB_DESKTOP=1 node tests/test-mobile-hubs-fixtures.mjs
node tests/test-report-tools-browser.mjs
REPORT_TOOLS_DESKTOP=1 node tests/test-report-tools-browser.mjs
HUB_AFTER_ONLY=1 HUB_EVIDENCE_DIR=docs/report-tools/hubs node tests/test-mobile-hubs-browser.mjs
SITE=.cache/mobile-hubs/site bash scripts/run-tests.sh
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www bash scripts/run-tests.sh
python3 scripts/release-report-tools.py
python3 tests/test-report-tools-packaging.py
python3 tests/test-mobile-hubs-packaging.py
python3 tests/test-fast-ui-packaging.py
```

وابستگی‌های تست PHP در `tests/package-lock.json` هستند. تست مرورگر از Playwright، @sparticuz/chromium و @axe-core/playwright در `.cache/browser/node_modules` استفاده می‌کند؛ در sandbox این اجرا، `LD_LIBRARY_PATH=$PWD/.cache/browser/lib/lib` نیز تنظیم شد. builder جدید فقط دو اصلاحی report-tools را می‌سازد؛ builderهای تاریخی را دوباره اجرا نکنید.
