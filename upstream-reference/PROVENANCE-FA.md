# مرجع بالادستی — کپی سورس `Maxess/mtagbot`

> **چرا این پوشه وجود دارد؟** تا دیگر نیازی به `git clone` ریپوی بالادستی نباشد.
> نقشهٔ کد و معماری اولیهٔ پلتفرم همیشه در دسترس است.

## مبدأ

| | |
|---|---|
| ریپو | https://github.com/Maxess/mtagbot |
| شاخه | `master` |
| commit | `2de63f746aa3bfe83c5c538cd860f75b716103bb` |
| تاریخ آخرین push | ۲۰۲۶-۰۸-۲۶ |
| تاریخ کپی‌برداری | ۲۰۲۶-۰۹-۱۲ |
| نسخهٔ پلتفرم | ۴.۲۸.x |

## چه چیزی اینجا هست

**۱۶۰ فایل / ۱.۸ مگابایت** — کل سورس:

- ۱۰۶ فایل PHP (۱۱۴۵ ک.ب) — همهٔ صفحات، `includes/`، `api/`، `config/`، `cron/`
- `sql/database.sql` (۳۸ ک.ب) — **۴۷ جدول** schema اصلی MySQL
- `assets/` — CSS و JS محلی
- ۴۳ فایل `.md` — گزارش‌های تحویل v4.12 تا v4.28 + `MEMORY-RESUME-v4.28.md`
- `7reporte.csv` — نمونهٔ فایل ایمپورت

## چه چیزی عمداً نیامده

| حذف‌شده | حجم | دلیل |
|---|---|---|
| `PROJECT-CODE-BUNDLE-v*.json` (۵۰ فایل) | ۶۶.۶ MB | اسنپ‌شات همان سورس — کاملاً تکراری |
| `code.zip` | ۲۲ MB | آرشیو همان سورس |

بدون این دو، حجم از **۹۰ MB به ۱.۸ MB** می‌رسد و هیچ اطلاعاتی از دست نمی‌رود:
هر دو فقط بسته‌بندیِ دوبارهٔ همان ۱۰۶ فایل PHP هستند.

اگر روزی به باندل خاصی نیاز شد، با یک `git clone` موقت قابل گرفتن است؛ ولی برای
خواندن کد و مقایسهٔ معماری، همین پوشه کافی است.

## نحوهٔ استفاده

**مقایسهٔ یک فایل با نسخهٔ فعلی:**
```bash
diff upstream-reference/online-exam-take.php \
     <مسیر فایل در بستهٔ دسکتاپ>/www/online-exam-take.php
```

**یافتن جدول‌های جدید نسبت به بالادستی:**
```bash
grep -oP '(?<=`)[a-z_]+(?=`)' upstream-reference/sql/database.sql | sort -u
```

**خواندن معماری از زبان خود پروژه:**
`MEMORY-RESUME-v4.28.md` و `SYSTEM-AUDIT-REPORT-v4.27.0.md`

> ⚠️ این پوشه **فقط خواندنی** است. هیچ اصلاحی اینجا انجام نمی‌شود — محل کار،
> سورس داخل بستهٔ دسکتاپ و پوشه‌های `update-v*/` است.
