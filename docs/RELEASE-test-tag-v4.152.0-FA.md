# «تگ آزمایشی» — برگهٔ آزمون سامانهٔ حضور و غیاب

نسخه سایت **4.152.0** و دسکتاپ **2.83.0** تغییر نمی‌کنند. این اصلاح فقط دو فایل را عوض می‌کند و کاری به داده، تنظیمات، قواعد حضور/تأخیر، اعلان اولیا و API ثبت ندارد.

## خواستهٔ کاربر

> «در بخش چاپ تگ، یک گزینه «تگ آزمایشی» اضافه شود که یک برگهٔ آزمایشی با دو تگ «حضور به موقع» و «تأخیر» چاپ کند؛ با اسکن آن‌ها هم سیستم حضور و غیاب/اسکنر تست و بررسی شود و هم پیام‌های آزمایشی مربوط به حضور/تأخیر برای حساب متصل‌شدهٔ مدیریت فرستاده شود — هم در تلگرام و هم برای بله — تا مدیریت مطمئن شود کل سامانه کار می‌کند.»

## چه چیزی اضافه شد

### ۱) دکمهٔ «تگ آزمایشی» در صفحهٔ چاپ تگ

در `attendance-tags.php` یک کارت تازه و یک دکمه در بالای صفحه اضافه شد: **«تگ آزمایشی»**. با زدن آن، همان نمای چاپ با پارامتر `test=1` باز می‌شود و یک برگهٔ کوچک با **دو تگ** چاپ می‌شود:

| تگ | محتوای QR |
|---|---|
| حضور به موقع | `MTAG-ATT-TEST:present` |
| تأخیر | `MTAG-ATT-TEST:late` |

- نام تگ («حضور به موقع» / «تأخیر»)، خط «تگ آزمایشی» و خط «بدون ثبت حضور و غیاب» **همیشه** زیر بارکد چاپ می‌شوند — حتی اگر طراح تگ آن خطوط را خاموش کرده باشد.
- اندازه، فاصله، حاشیه، کادر برش، پلاک مدرسه، فونت و تعداد نسخه دقیقاً همان تنظیمات ویرایشگر زندهٔ تگ است؛ پس برگه با همان ظاهر واقعی تگ‌ها چاپ می‌شود.
- هیچ دانش‌آموزی خوانده نمی‌شود و هیچ توکنی ساخته نمی‌شود؛ برگهٔ آزمایشی کاملاً بی‌خطر است.
- در کارت راهنما، وضعیت اتصال حساب‌ها هم نشان داده می‌شود («بله: ۲ حساب متصل | تلگرام: بدون حساب متصل») تا پیش از چاپ بدانید پیام آزمایشی به جایی می‌رسد یا نه. بعد از اسکن، خط «آخرین آزمون …» شمار پیام‌های ارسالی روی هر پیام‌رسان را نشان می‌دهد.

### ۲) اسکن تگ آزمایشی: همان مسیر واقعی، بدون هیچ ثبتی

پردازش تگ آزمایشی داخل هستهٔ حضور و غیاب (`includes/attendance_helpers.php` → `att_process_test_scan`) انجام می‌شود، پس **همان مسیر واقعی** طی می‌شود: دوربین و کنترلر اسکنر → `attendance-scan-api.php` (با همان کلید اسکنر و همان احراز هویت) → پردازش → صف ماندگار اعلان‌ها. فقط نتیجهٔ نهایی متفاوت است:

- هیچ رکوردی در `student_attendance` نوشته نمی‌شود (نه حضور، نه تأخیر، نه غیبت).
- هیچ تگ/توکنی برای دانش‌آموزان ساخته یا باطل نمی‌شود.
- **هیچ پیامی برای والدین نمی‌رود.**
- روی صفحهٔ اسکنر، نتیجه با همان قالب همیشگی نمایش داده می‌شود ولی صریحاً آزمایشی است: نام «حضور به موقع — تگ آزمایشی» یا «تأخیر — تگ آزمایشی» و زیرنویس «آزمون سامانه — بدون ثبت حضور و غیاب».

### ۳) پیام آزمایشی برای حساب مدیریت — هم بله، هم تلگرام

گیرندگان، حساب‌های مدیریتی متصل به ربات روی **هر دو پیام‌رسان** هستند:

- مدیر متصل‌شده به ربات (`bot_admin_sessions.role_type = 'admin'`)،
- کارکنان دارای نقش مدیریتی: **معاون/ناظم** و **معاون اجرایی**.

اگر روی یک پیام‌رسان هیچ حساب مدیریتی متصل نباشد، برای اینکه آزمون بی‌اثر نماند، پیام به چت‌های فعال کارکنان همان پیام‌رسان می‌رود و این مورد در نتیجهٔ آزمون «(کارکنان)» علامت می‌خورد. چت‌های غیرفعال هرگز پیام نمی‌گیرند و پیام تکراری برای یک نفر (مثلاً کسی که هم معاون است هم معاون اجرایی) فقط یک‌بار فرستاده می‌شود.

نمونهٔ متن پیام (تأخیر، روی بله):

```text
🧪 آزمون سامانهٔ حضور و غیاب — پیام نمونه روی بله
⏰ اطلاع‌رسانی تأخیر (نمونهٔ آزمایشی)
وضعیت آزمایشی: تأخیر
فرزند شما امروز با ۱۲ دقیقه تأخیر در مدرسه حاضر شده است.
ساعت دقیق ورود به مدرسه: 08:52
تاریخ: 1405/06/29
دانش‌آموز: — (تگ آزمایشی، بدون دانش‌آموز واقعی)

این پیام نمونهٔ آزمایشی است و با اسکن «تگ آزمایشی» فرستاده شده؛ هیچ حضور یا غیابی ثبت نشده و برای والدین ارسال نشده است.
```

- متن، **همان متن واقعی والدین** است (همان سرلوحهٔ حضور/تأخیر و همان تعداد دقیقهٔ تأخیر) با سرلوحه و پانویس «آزمایشی»؛ نام پیام‌رسان در خط اول می‌آید تا معلوم باشد پیام از بله رسیده یا تلگرام.
- ارسال از همان مسیر تولیدی (صف ماندگار `bot_outbox` و لاگ پیام‌ها با نوع `attendance_test`) انجام می‌شود؛ یعنی آزمون، سلامت همان مسیری را می‌سنجد که در روز واقعی استفاده می‌شود.
- نتیجهٔ هر آزمون در تنظیمات `att_test_last_result` ذخیره می‌شود و در کارت «تگ آزمایشی» همین صفحه نشان داده می‌شود.

### ۴) امنیت قالب تگ واقعی

قالب تگ دانش‌آموزان و پارسر آن **دست‌نخورده** است (`MTAG-ATT:{id}:{token}` با همان regex). پیش‌شمارهٔ تازه فقط `MTAG-ATT-TEST:` است و هر رشتهٔ دیگری (مثلاً `MTAG-ATT-TEST:7201` یا `MTAG-ATT-TEST:late:1`) نامعتبر شمرده می‌شود و هیچ پیامی نمی‌فرستد.

## نصب

| گزینه | بسته | توضیح |
|---|---|---|
| فقط همین قابلیت | `SITE-FIX-v4.152.0-attendance-test-tag.zip` + `SchoolDeskPro-FIX-v2.83.0-attendance-test-tag.zip` | دو بستهٔ اصلاحی؛ محتوای سمت سایت زیر `site-update-v4.152.0/` و سمت دسکتاپ زیر `SchoolDeskPro/reports/` |
| همهٔ اصلاح‌ها یک‌جا | `SITE-FIX-v4.152.0-cumulative.zip` + `SchoolDeskPro-FIX-v2.83.0-cumulative.zip` | همان دو بستهٔ جمعی که اسکنر `camera9` و به‌روزرسانی آنلاین دسکتاپ را هم دارد و این قابلیت را هم شامل می‌شود |
| به‌روزرسانی آنلاین دسکتاپ | `SchoolDeskPro-UPDATE-2.83.0-attendance-test-tag.zip` | از صفحهٔ «به‌روزرسانی دسکتاپ» سایت بارگذاری و روی همان دستگاه نصب می‌شود (همان دو فایل + `DESKTOP-UPDATE.json`) |

روش نصب همان روش همیشگی بسته‌های اصلاحی است: فایل‌های داخل ZIP روی ریشهٔ نصب رونویسی می‌شوند (بدون تغییر دیتابیس و بدون اجرای مهاجرت).

## فایل‌های همین بسته

| فایل داخل بسته | بایت | SHA256 |
|---|---|---|
| `attendance-tags.php` | 40723 | `933509f41b4103f25bdf6ddf6bbcc037b162c36bd8c692777692dba8976e87d0` |
| `includes/attendance_helpers.php` | 34566 | `a6d1bc736ccb98bc57c652e92490521f251742e7226e15850db1ce8adcbdf38d` |

همین دو بایت دقیقاً در بستهٔ سایت، بستهٔ دسکتاپ، بستهٔ آنلاین، بستهٔ جمعی و هر دو Release قرار دارند (آزمون بسته‌بندی همین را می‌سنجد).

## بسته‌های جاری و هش‌ها

| فایل | بایت | SHA256 |
|---|---|---|
| `SITE-FIX-v4.152.0-attendance-test-tag.zip` | 22215 | `57c0d29b9d9cf4b060111d8c219b709ad59bb4a00b03c7b923b1087f29eaf3e7` |
| `SchoolDeskPro-FIX-v2.83.0-attendance-test-tag.zip` | 22219 | `6542be1f4fc75ccc1aaac760da908f26c6450526e482ec3ee85cdd27a337f446` |
| `SchoolDeskPro-UPDATE-2.83.0-attendance-test-tag.zip` | 22852 | `34c5ddde1540f54540781a5e25b4312821ab10e8cb5d7894a6201b17dd1ce701` |
| `SITE-FIX-v4.152.0-cumulative.zip` | 71759 | `492c71148e6b7be0e4f33bf550867dc9e83174b99f3401f1bfb5f9dc1c9bee13` |
| `SchoolDeskPro-FIX-v2.83.0-cumulative.zip` | 101206 | `cc6f270ce2063fa22a565b9dabe3607ca6457f14228073f1266a444ba808b7c3` |
| `Release_V1.0-Site.zip` | 3493429 | `bc47c78025c609cded2573aaf880880b7928f5d21a1d780002769f0c74aa7794` |
| `Release_V1.0-Desktop.zip` | 17116379 | `16c1da262f677090ff9e7a01481d288967dd921b7c6612d677a3ec2a44c46993` |
| `SITE-FIX-v4.152.0-scanner.zip` | 36924 | `87e68f0e5b2b60cad5eb0888a30ffc7d8f280838a421ac6c77b17de0516357b5` |
| `SchoolDeskPro-FIX-v2.83.0-scanner.zip` | 36932 | `69c8744718a425444eec82f6cec28d44e08227bc83065ce2fbda5b79da76b7f5` |
| `SchoolDeskPro-UPDATE-2.83.0-scanner-focus.zip` | 37599 | `14ae783f7456b073f0df9b969e736548b5e7c3c2a0b4bbdfd43e9a8b6b491a6e` |
| `SITE-FIX-v4.152.0-desk-update.zip` | 12664 | `99366e9daeb0278b323ae1d696a435702c87bc0b57807d0d82f228e4b88e4fae` |
| `SchoolDeskPro-FIX-v2.83.0-desk-update.zip` | 42099 | `06a6de4284d0ba22cdb3197cfbb82b0aebc39da4057e4c51e6b2c274f7fcbcaf` |

هش دو بستهٔ اصلاحی قبلی (`scanner` و `desk-update`) با نوبت قبل یکی است؛ هیچ ZIP تاریخی بازسازی یا تغییر نکرده است.

## آزمون‌های همین اصلاح

- **۵۲ بررسی تازهٔ PHP/SQLite** در `tests/test-attendance-test-tag.mjs`: چاپ برگهٔ آزمایشی (دقیقاً دو تگ با همان دو payload، نام‌ها و خط «بدون ثبت حضور و غیاب»، نبود هیچ دانش‌آموز/توکن واقعی روی برگه، تکرار نسخه‌ها، و بی‌تغییری برگهٔ عادی دانش‌آموزان)، اجرای واقعی `attendance-scan-api.php` با کلید اسکنر برای هر دو تگ، نبود هر رکورد در `student_attendance` و نبود تگ تازه، رسیدن پیام به مدیر بله و معاون تلگرام (و نرسیدن به والدین/نشست‌های غیرفعال)، وضعیت `sent` در صف ماندگار و لاگ، خط «آخرین آزمون» در صفحه، رد payloadهای نامعتبر بدون هیچ ارسال، و اینکه تگ واقعی دانش‌آموز همچنان یک رکورد ثبت می‌کند و اعلان والدین را می‌فرستد.
- **۷ بررسی بسته‌بندی** در `tests/test-attendance-test-tag-packaging.py`: فقط همان دو فایل در هر بسته، یکسان‌بودن سایت و دسکتاپ، مانيفست بستهٔ آنلاین (نسخه/حجم/هش و رد فایل‌های ممنوع)، درون‌بودن همین بایت‌ها در بستهٔ جمعی و هر دو Release، سیم‌کشی سازنده‌ها، و بودن گزینه در همان بایت‌های ارسالی.
- **۳۳/۳۳ سوئیت پروژه سبز** (شامل ۸۳ سناریوی کنترلر اسکنر و ۱۱ سناریوی صفحهٔ اسکنر)، به‌همراه **۵۸ بررسی نصب کامل**، **۴۴ بررسی چیدمان گزارش‌ها** و **۱۱ گروه بسته‌بندی Release**؛ همه روی همان بسته‌هایی که در جدول بالا آمده‌اند.
- آزمون با بله/تلگرام واقعی انجام نشده است (متن‌ها و مسیر ارسال با mock قطعی سنجیده شده‌اند)؛ صحت نهایی با همان برگهٔ آزمایشی روی گوشی و حساب واقعی مدیریت دیده می‌شود.
