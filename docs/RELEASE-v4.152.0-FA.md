# تحویل اصلاح‌شدهٔ 4.152.0 سایت / 2.83.0 دسکتاپ — فقط فایل‌های لازم

تاریخ بازبسته‌بندی: ۲۰۲۶-۰۹-۱۵ — شاخه: `arena/01a0a1d9-baci`

طبق درخواست، **نسخه تغییر نکرده و ZIPهای قبلی در همان آدرس جایگزین شده‌اند**.
بستهٔ سایت دیگر تجمیعی نیست و بستهٔ دسکتاپ دیگر برنامهٔ کامل نیست.
کد برنامه در این بازبسته‌بندی تغییر نکرده است.

## دانلود

- [فقط فایل‌های اصلاحات سایت — حدود ۴۴ کیبی‌بایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-UPDATE-v4.152.0.zip)
- [فقط فایل‌های اصلاحات دسکتاپ — حدود ۴۴ کیبی‌بایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-v2.83.0-win64.zip)
- [لینک جایگزین فایل‌های سایت؛ همان چهار فایل](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/MODIFIED-FILES-v4.152.0.zip)

ریپو خصوصی است؛ برای دانلود در مرورگر، وارد حساب GitHub دارای دسترسی شوید.

## محتویات دقیق هر ZIP

هر بسته فقط این چهار فایل جدید/اصلاح‌شده را دارد:

```text
reports-lists.php
includes/docx_class_list.php
includes/docx_school_list.php
assets/templates/school-students.docx
```

فایل اجرایی، PHP runtime، تنظیمات، دیتابیس، `data` و حتی راهنما داخل ZIPها نیستند.
این صفحه و `update-v4.152.0/راهنمای-بروزرسانی.txt` راهنمای نصب هستند.

## نصب روی نسخهٔ موجود

**پیش‌نیاز:** سایت 4.151.0 یا نسخهٔ فعلی 4.152.0؛ دسکتاپ 2.82.0 یا نسخهٔ فعلی 2.83.0.
برای نصب مستقل یا جبران نسخه‌های میانی قدیمی‌تر از این ZIPها استفاده نکنید.

### سایت

۱. پشتیبان بگیرید و ZIP سایت را باز کنید.
۲. محتویات پوشهٔ `site-update-v4.152.0` را در ریشهٔ سایت موجود کپی کنید، نه خود پوشه را.
۳. مسیرهای `includes` و `assets/templates` را حفظ و فقط فایل‌های هم‌نام را جایگزین کنید.
۴. اگر از لینک جایگزین MODIFIED-FILES استفاده می‌کنید، محتویات `update-v4.152.0` را کپی کنید؛ نصب هر دو لازم نیست.

### دسکتاپ

۱. برنامه را کامل ببندید و از پوشهٔ نصب‌شده خصوصاً `data` پشتیبان بگیرید.
۲. محتویات `SchoolDeskPro/www` داخل ZIP را در پوشهٔ `www` برنامهٔ موجود کپی کنید.
۳. زیرپوشه‌ها را **ادغام** و فقط فایل‌های هم‌نام را جایگزین کنید؛ کل `www` را حذف نکنید.
۴. به `data`، دیتابیس، تنظیمات و فایل اجرایی دست نزنید. انتقال data یا نصب مجدد لازم نیست.

این ZIP دسکتاپ به‌تنهایی قابل اجرا نیست؛ صرفاً وصلهٔ برنامهٔ موجود است.

## خلاصهٔ تغییرات نرم‌افزار (بدون تغییر نسبت به تحویل قبلی)

- PDF دبیر: کد `1/7` با جهت مستقل LTR و ردیف عنوان/کلاس ۸ میلی‌متری؛ Word دبیر تغییر نکرده است.
- تب «لیست دانش‌آموزان کل مدرسه»: Word از قالب اصلی، نام خانوادگی در راست و نام در ستون بعدی.
- سال پیش‌فرض، کلاس‌های پویا، جمع هر پایه و کل مدرسه و صفحات ادامه بدون حذف اسامی.
- قالب `2 Titr` اصلی را حفظ می‌کند؛ فایل TTF آن هنوز جاسازی نشده و باید روی دستگاه مقصد نصب باشد.

## کنترل کیفیت

آزمون‌های انتشار قبلی روی کد همین نسخه: **۹۰۲ PASS، صفر FAIL، دو SKIP** در ۱۹ سوئیت.
تست Chromium واقعی نیز ترتیب کلاس، ارتفاع عنوان، بارگذاری B Titr و دو صفحهٔ A4 PDF را تأیید کرد.
Word ویندوز/LibreOffice در محیط آزمون موجود نبودند؛ تأیید بصری DOCX در آن‌ها انجام نشده است.

کنترل بازبسته‌بندی:
- هر ۱۹ سوئیت دوباره روی نسخهٔ کامل 2.82 با وصلهٔ فعلی اجرا و سبز شدند؛ چهار تست مستقل بسته‌بندی نیز پاس شدند.
- هر سه ZIP دقیقاً چهار ورودی دارند؛ CRC و برابری بایت‌به‌بایت با چهار فایل سورس بررسی می‌شود.
- کد PHP و قالب نسبت به تحویل قبلی هیچ تغییری ندارند.
- بسته‌های فقط-وصله، خودکار به‌عنوان نسخهٔ کامل توسط هارنس تست استفاده نمی‌شوند؛ مسیر نسخهٔ کامل را صریح بدهید:

```sh
npm ci --prefix tests
SITE=/absolute/path/to/installed/SchoolDeskPro/www PATCH=update-v4.152.0 bash scripts/run-tests.sh
python3 scripts/release-rosters.py
python3 tests/test-roster-packaging.py
```

## SHA-256 بسته‌های جایگزین

```text
ae38762657ec7f41a3ce58b725ad8cb65df3342a76f143511beebf9d9cd3b093  MODIFIED-FILES-v4.152.0.zip
a4fe83fc38f68b9cba51d8a93589a5490754efbaf8265d1178e39305a0782076  SITE-UPDATE-v4.152.0.zip
b1aa69d4e4a75b76f3bf1611f9ac64e675bd655c90b4e617e0c0901fc9bc68b9  SchoolDeskPro-v2.83.0-win64.zip
```
