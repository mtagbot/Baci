# گزارش بررسی Release_V1.0

تاریخ ساخت نخستین بسته: 2026-09-17؛ آخرین بازسازی: 2026-09-20 (بازسازی ششم: «تگ آزمایشی» — برگهٔ آزمون سامانه + پیام آزمایشی **فقط برای حساب مدیریت** روی بله و تلگرام؛ شامل اصلاح اسکنر `camera9` از بازسازی پنجم). هدف: دو نصب کامل از صفر، با تغییرات سایت تا 4.152.0 و دسکتاپ تا 2.83.0.

## بازسازی ششم ۲۰ سپتامبر ۲۰۲۶ (تگ آزمایشی — برگهٔ آزمون سامانه)

- در بخش چاپ تگ، گزینهٔ **«تگ آزمایشی»** اضافه شد: یک برگهٔ چاپی کوچک با دو تگ «حضور به موقع» و «تأخیر» (payloadهای `MTAG-ATT-TEST:present` و `MTAG-ATT-TEST:late`). نام تگ‌ها، خط «تگ آزمایشی» و خط «بدون ثبت حضور و غیاب» همیشه چاپ می‌شوند و اندازه/فاصله/پلاک/فونت همان تنظیمات ویرایشگر زنده است.
- اسکن هر تگ آزمایشی **همان مسیر واقعی** را طی می‌کند (کنترلر اسکنر → `attendance-scan-api.php` با کلید اسکنر → هستهٔ حضور و غیاب → صف ماندگار اعلان‌ها)، ولی هیچ رکوردی در `student_attendance` نوشته نمی‌شود، هیچ تگی ساخته/باطل نمی‌شود و هیچ پیامی برای والدین نمی‌رود.
- پیام آزمایشی — با متن نمونهٔ همان اعلان واقعی و پانویس «آزمایشی» — **فقط** برای همان حسابی فرستاده می‌شود که مدیر خودش از طریق ربات با نام کاربری و رمز، حسابش را به ربات اضافه کرده است (`bot_admin_sessions` فعال با `role_type = 'admin'`) روی هر یک از دو پیام‌رسان بله و تلگرام. هیچ نقش دیگری (دبیر، معاون/ناظم، معاون اجرایی، مشاور) و هیچ والدی پیام نمی‌گیرد و **هیچ جانشینی (fallback) هم وجود ندارد**: پیام‌رسانی که حساب مدیریت ندارد «بدون حساب مدیریت متصل» گزارش می‌شود و چیزی برای آن فرستاده نمی‌شود. نتیجه در `att_test_last_result` ذخیره می‌شود و در همان صفحهٔ چاپ تگ نمایش داده می‌شود.
- این اصلاح فقط دو فایل را عوض می‌کند: `attendance-tags.php` و `includes/attendance_helpers.php`. بسته‌های تازه: `SITE-FIX-v4.152.0-attendance-test-tag.zip` (۲۲٬۲۹۰ بایت، `2d967419bc946fd62fd41e4b6ab1c6008b147310bd09ccc77d7d1f71cc269fca`)، `SchoolDeskPro-FIX-v2.83.0-attendance-test-tag.zip` (۲۲٬۲۹۴ بایت، `3e78b6e84d82507e3c955274167d78738355b76aa6e838af43a585b6abb4c900`) و بستهٔ آنلاین `SchoolDeskPro-UPDATE-2.83.0-attendance-test-tag.zip` (۲۳٬۰۴۲ بایت، `6ebb96db98644963e4b9527cb2f60fe275422894c2229ccca0439ad888eb0867`).
- بسته‌های تجمیعی بازسازی شدند تا همین قابلیت را هم داشته باشند: `SITE-FIX-v4.152.0-cumulative.zip` (۷۱٬۸۳۴ بایت، `8e54edade21a622cbc8a3ab65a8a8dd79c16df992459028f8e4b159623ddabcb`) و `SchoolDeskPro-FIX-v2.83.0-cumulative.zip` (۱۰۱٬۲۸۱ بایت، `aabbca107cdbe929dd3f458a5563adcd8a17564635d32409fc5026641d31ffb2`)؛ هش دو بستهٔ اسکنر، دو بستهٔ به‌روزرسانی دسکتاپ و بستهٔ آنلاین تمرکز دوربین با نوبت قبل **یکسان** ماند (هیچ ZIP تاریخی بازسازی نشد).
- خروجی‌ها: `Release_V1.0-Site.zip` (۳٬۴۹۳٬۵۰۹ بایت، `7d737045837a5683ec300acef7aa905dbc2e4796438a26ce2e5ba0bb5c36c888`)، `Release_V1.0-Desktop.zip` (۱۷٬۱۱۶٬۴۵۷ بایت، `6e9c4c2663ee5b9aff2fd295efe129097e16c0228d8cc29a99a5866fdede4e3c`) و `Release_V1.0-SHA256SUMS.txt` (بازنویسی شد).
- آزمون‌ها روی همین بسته‌ها: **۳۳/۳۳ سوئیت پروژه** (شامل ۶۰ بررسی تازهٔ تگ آزمایشی — با کنترل منفی روی بستهٔ پیشین —، ۸۳ سناریوی کنترلر اسکنر و ۱۱ سناریوی صفحهٔ اسکنر)، ۸ بررسی بسته‌بندی تگ آزمایشی، ۵۸ بررسی نصب تازه، ۴۴ بررسی چیدمان گزارش‌ها، ۱۱ گروه بسته‌بندی Release و ۷ گروه بستهٔ تجمیعی.
- هنوز آزمایش‌نشده: ارسال واقعی پیام آزمایشی به بله/تلگرام و اجرای آن روی گوشی فیزیکی (مانند گذشته).

## بازسازی پنجم ۱۹ سپتامبر ۲۰۲۶ (اسکنر camera9 — نردبان ترمیم خودکار)

- اسکنر حالا **نتیجهٔ خودش را می‌سنجد**: تا وقتی در یک نشست صفحه هیچ تگی خوانده نشده باشد، نردبان ترمیم را پله‌پله ادامه می‌دهد — ۲.۵ ثانیه بازپیوند جریان، ۵ ثانیه بازکردن همان دوربین با شناسهٔ دقیق، ۸ ثانیه **تعویض موقت دوربین و برگشت** (همان کار دستی اپراتور، خودکار)، ۱۱ ثانیه ساده‌ترین تنظیمات و ۱۴ ثانیه آزادکردن قفل لنز — و بعد با فاصله‌های فزاینده (۱۱، ۲۰، ۳۰ ثانیه) تکرار می‌کند و رها نمی‌کند.
- به‌محض خوانده‌شدن اولین تگ همه‌چیز می‌ایستد؛ از آن به بعد فقط تصویر واقعاً خراب (سیاه/یخ‌زده/مسیر مرده) ترمیم می‌شود و یک سیاهی کوتاه هم دوربین را باز نمی‌کند. حلقهٔ تحلیل دیده‌بان دارد (صفحهٔ «آماده» بدون حلقهٔ فعال، دوباره راه‌اندازی می‌شود) و ترتیب راه‌اندازی سخت شد: اول حلقهٔ تحلیل، بعد فهرست دوربین و تنظیم لنز.
- نشست ترمیم‌شده قفل ۱۰–۳۰ سانتی‌متر را نگه می‌دارد ولی شاتر کوتاه نمی‌گیرد؛ جعبهٔ «آمادهٔ اسکن» هم دیگر ثابت نیست و تا آماده‌شدن دوربین «در حال باز کردن دوربین…» نشان می‌دهد. برای گزارش روی گوشی، `?debug=1` یک نوار وضعیت زنده پایین صفحه می‌گذارد.
- خروجی‌ها: `Release_V1.0-Site.zip` (۳٬۴۸۹٬۱۹۲ بایت، `23abf44470069bf30eb7f8868e5a5422c935e0453c8cbf308e50d2b2e6c3b4d0`)، `Release_V1.0-Desktop.zip` (۱۷٬۱۱۲٬۱۳۰ بایت، `1bf5a1ff78c46cd94fc8e7984f079ad1748326022502b079e9d58ae5a5f08219`) و `Release_V1.0-SHA256SUMS.txt` (بازنویسی شد).
- دو بستهٔ تجمیعی تازه: `SITE-FIX-v4.152.0-cumulative.zip` (۴۹٬۵۶۶ بایت، `961c95ead4f41a68bd649b831eaa01990dc69441cbc180c0fb28f4bd6b8a8a96`) و `SchoolDeskPro-FIX-v2.83.0-cumulative.zip` (۷۹٬۰۰۹ بایت، `7d5ad4d2701af876e758f7f6e4183bd0fab2c4d0181993cbac6994fe81215fc3`)؛ بستهٔ به‌روزرسانی خودکار دسکتاپ بدون تغییر ماند.
- آزمون‌ها روی همین بسته‌ها: ۸۳ سناریوی کنترلر اسکنر، ۱۱ سناریوی صفحه/احراز هویت، ۷۴ بررسی به‌روزرسانی/نصب خودکار، ۵۸ بررسی نصب تازهٔ دسکتاپ، ۴۴ بررسی چیدمان گزارش‌ها، ۳۲/۳۲ سوئیت پروژه و همهٔ گروه‌های بسته‌بندی (اسکنر ۵، به‌روزرسانی ۱۰، تجمیعی ۷، انتشار کامل ۱۱).
- هنوز آزمایش‌نشده: اجرای واقعی روی ویندوز/گوشی فیزیکی (مانند گذشته).

## بازسازی چهارم ۱۹ سپتامبر ۲۰۲۶ (اسکنر camera8 — اسکن از نخستین باز شدن صفحه)

- ریشهٔ «اسکن نشدن تا تعویض دستی دوربین» بسته شد: اگر حدود ۵ ثانیه از شروع حلقهٔ تحلیل بگذرد و هیچ تگی خوانده نشده باشد، اسکنر یک‌بار در هر نشست صفحه همان دوربین را با **شناسهٔ دقیق** بسته و دوباره باز می‌کند (همان کاری که تعویض دستی می‌کرد و قفل لنز هم روی نشست تازه دوباره اعمال می‌شود)؛ و نگهبان تازهٔ «خط تولید تصویر» تصویر سیاه/یخ‌زده/غیرقابل‌خواندن را با نردبان بازپیوند جریان → بازکردن همان دوربین → تنظیمات ساده‌تر ترمیم می‌کند. پرچم `muted` هم دیگر جلوی ترمیم را نمی‌گیرد.
- خروجی‌ها: `Release_V1.0-Site.zip` (۳٬۴۸۶٬۸۱۱ بایت، `7e00cd1111fea908d1a2d79368f8833fec60c5f590bbbf344703856126611697`)، `Release_V1.0-Desktop.zip` (۱۷٬۱۰۹٬۷۵۰ بایت، `0deca4adda6829f35e6cf0b9bd3d67a8913feb2f71ab377514cf79e99d4e3b42`) و `Release_V1.0-SHA256SUMS.txt` (بازنویسی شد).
- دو بستهٔ تجمیعی تازه: `SITE-FIX-v4.152.0-cumulative.zip` (۴۷٬۱۸۱ بایت، `27720e833f009c01a66a34109725b03e56b18148c0026cfc8faf81047b52cfe9`) و `SchoolDeskPro-FIX-v2.83.0-cumulative.zip` (۷۶٬۶۲۴ بایت، `622816d20aeb180264203dc02f89c42636a5df0ee749240691bf0448925a94a7`)؛ بستهٔ به‌روزرسانی دسکتاپ بدون تغییر ماند.
- آزمون‌ها روی همین بسته‌ها: ۷۷ سناریوی کنترلر اسکنر، ۱۱ سناریوی صفحه/احراز هویت، ۷۴ بررسی به‌روزرسانی/نصب خودکار، ۵۸ بررسی نصب تازهٔ دسکتاپ، ۴۴ بررسی چیدمان گزارش‌ها، ۳۲/۳۲ سوئیت پروژه و همهٔ گروه‌های بسته‌بندی (اسکنر ۵، به‌روزرسانی ۱۰، تجمیعی ۷، انتشار کامل ۱۱).
- هنوز آزمایش‌نشده: اجرای واقعی روی ویندوز/گوشی فیزیکی (مانند گذشته).

## بازسازی سوم ۱۹ سپتامبر ۲۰۲۶ (اسکنر camera7 + به‌روزرسانی خودکار دسکتاپ)

- دو بستهٔ تجمیعی به‌روز شدند: `SITE-FIX-v4.152.0-cumulative.zip` (۴۵٬۵۷۰ بایت، `fa6a45511c24d4a0dd25e392ed7ba50372f36320c7875e0411f3425bd8802dec`) و `SchoolDeskPro-FIX-v2.83.0-cumulative.zip` (۷۵٬۰۱۳ بایت، `27d7e5fa663f750b518db6bd2a6f3a0c3e404ddb4942f640ba5d5592f783ac59`).
- اسکنر `camera7`: بازهٔ فوکوس ۱۰–۳۰ سانتی‌متر (پارک روی ۲۰)، آماده‌شدن دوربین بدون تکیه بر `paused`/`readyState>=2`، تعمیر خودکار دوربین (بازکردن دوبارهٔ همان دوربین پس از ۷ ثانیه بدون فریم و پس از ۶ ثانیه بدون هیچ فریمِ تحلیل‌شده)، لایهٔ نام/وضعیت ۳۸ پیکسل بالاتر از لبهٔ پایین و `height:100dvh` برای مرورگرهای موبایل.
- به‌روزرسانی دسکتاپ: دیمن همگام‌سازی هر چرخه (۹۰۰ ثانیه) نسخهٔ سایت را بررسی و بستهٔ تازه را خودکار نصب می‌کند؛ صفحهٔ «همگام‌سازی با سایت» نشان وضعیت را نشان می‌دهد (`desk_update_status()` با کد/برچسب/رنگ) و صفحهٔ «به‌روزرسانی نرم‌افزار» فقط پشتیبان دستی است.
- خروجی‌ها: `Release_V1.0-Site.zip` (۳٬۴۸۵٬۱۹۸ بایت، `3c136321fe24a941799eb3e180dc22a7c4254206ab11fd916e65e553216595d4`)، `Release_V1.0-Desktop.zip` (۱۷٬۱۰۸٬۱۳۸ بایت، `f8009abe8779ab9f9280d48d4e3117a1e3b307144c40ecb920fa5a851f88a570`) و `Release_V1.0-SHA256SUMS.txt` (بازنویسی شد).
- آزمون‌ها روی همین بسته‌ها: ۷۲ سناریوی کنترلر اسکنر، ۱۱ سناریوی صفحه/احراز هویت، ۷۴ بررسی به‌روزرسانی/نصب خودکار، ۵۸ بررسی نصب تازهٔ دسکتاپ، ۴۴ بررسی چیدمان گزارش‌ها، ۳۲/۳۲ سوئیت پروژه و همهٔ گروه‌های بسته‌بندی (اسکنر ۵، به‌روزرسانی ۱۰، تجمیعی ۷، انتشار کامل ۱۱).
- هنوز آزمایش‌نشده: اجرای واقعی روی ویندوز/گوشی فیزیکی (مانند گذشته).

## بازسازی دوم ۱۹ سپتامبر ۲۰۲۶ (اصلاح اسکنر + به‌روزرسانی آنلاین دسکتاپ)

- دو بستهٔ اصلاحی تازه منتشر و در زنجیرهٔ ساخت ثبت شدند: `scanner` (قفل فوکوس روی بی‌nهایت، جایگزین نسخهٔ «فوکوس نزدیک» با همان نام) و `desk-update` (به‌روزرسانی آنلاین نرم‌افزار دسکتاپ از روی سایت). هر دو **آخرین** حلقهٔ ترتیب ادغام‌اند تا هیچ تغییر قدیمی‌تری روی‌شان نوشته نشود.
- ترتیب ادغام اکنون: `svg-responsive` → `student-workflow` → `preview-navigation` → `fast-ui` → `mobile-hubs` → `report-tools` → `settings-health` → (دسکتاپ: `reports-layout`) → `bot-outbox` → (دسکتاپ: `optimized-sync` → `event-sync`) → `recovery-ui` → **`scanner`** → **`desk-update`**. یعنی اکنون **۱۰ بستهٔ اصلاحی سایت** و **۱۳ بستهٔ اصلاحی دسکتاپ** درون بسته است.
- خروجی‌ها: `Release_V1.0-Site.zip` (۳٬۴۸۳٬۷۲۵ بایت، `d2e287178e3247b2aa22cb7bd0b6e126001c2ed24fee736e6f17329fce80a903`)، `Release_V1.0-Desktop.zip` (۱۷٬۱۰۲٬۹۲۳ بایت، `18bc74f4c6ded4b6fa06c8465e05a59e67b30494c3885546e11dd3349877ae8c`) و `Release_V1.0-SHA256SUMS.txt` (بازنویسی شد).
- **۳۲ سوئیت رگرسیون روی پوشهٔ stage دسکتاپ و سایت** سبز است (سوئیت تازهٔ «به‌روزرسانی آنلاین دسکتاپ» اضافه شده)، **۱۱ گروه آزمون بسته‌بندی کامل** سبز، **۵۸ بررسی نصب تازهٔ دسکتاپ** سبز، و **۹ آزمون بستهٔ به‌روزرسانی دسکتاپ** به‌همراه ۵ آزمون بستهٔ اسکنر سبز.
- دو نکتهٔ ساخت‌وساز: پوشهٔ `uploads/` هرگز در بسته نمی‌رود (به همین دلیل محافظ `uploads/desktop-updates/.htaccess` در زمان اجرا و با بازشدن صفحهٔ انتشار ساخته می‌شود)، و `SchoolDeskPro.exe` همیشه از `launcher.c` کامپایل می‌شود.
- نسخهٔ داخلی‌ها همان سایت 4.152.0 / دسکتاپ 2.83.0 است؛ این دو اصلاح، اصلاحی‌اند نه نسخهٔ کامل.

## آنچه اجرا شد

- **۳۱ سوئیت رگرسیون روی خروجی کامل سایت و ۳۱ سوئیت روی خروجی کامل دسکتاپ**؛ PHP واقعی 8.3.33 در WebAssembly و SQLite داخل حافظه. دادهٔ آزمون فقط پس از اجرای نصب‌یار واقعی، داخل محیط مجزای آزمون ساخته می‌شود و وارد ZIP نمی‌شود.
- **۱۱۲ بررسی نصب/امنیت/یکپارچگی**: نصب تازهٔ SQLite؛ نصب تازه روی **MySQL واقعی 5.7.29**؛ CSRF، رمز ضعیف، کلید مالک هاست، اطلاعات اتصال اشتباه، عدم fallback، ایجاد تنها مدیر انتخابی، عدم وجود اطلاعات مدرسهٔ نمونه، قفل نصب مجدد و `force=1`، حفظ تنظیمات، تکرار مهاجرت‌ها، کلید تصادفی جدید، احراز هویت هر دو API همگام‌سازی، ورود واقعی مدیر، صفحات اصلی، کنترل مسیرهای خصوصی روتر دسکتاپ، تولید PDF واقعی با فونت فارسی و رد تنظیمات ناقص دیتابیس.
- **۱۱ گروه آزمون بسته‌بندی کامل**: فایل‌های ضروری، نبود اطلاعات و اسرار نصب‌شده، اسکیمای خالی و فعال‌ماندن triggerها، شاخص صحیح MySQL، انطباق بایتی آخرین فایل‌های تمام وصله‌ها، حفظ تفاوت‌های مستند دسکتاپ در مبنای کامل، حذف کلید تاریخی مشترک، SHA256 تک‌تک فایل‌ها، مسیرهای ایمن ZIP، ساختار اجرایی PE32+ ویندوز و درون‌بودن بسته‌های اصلاحی منتشرشده.
- **۹ سوئیت بسته‌بندی تاریخی** نیز بدون بازسازی یا تغییر ZIPهای تاریخی عبور کردند.
- لانچر از سورس فعلی با Zig 0.14.1 برای Windows x64 ساخته شد؛ فرمت PE، معماری و زیرسیستم GUI کنترل شد. این بررسی جای اجرای واقعی روی ویندوز را نمی‌گیرد.
- وابستگی TCPDF 6.11.4 با شناسهٔ blobهای Git تثبیت‌شده و Chart.js 4.5.1 با SHA256 تثبیت‌شده به‌صورت محلی همراه بسته قرار گرفتند.

## اصلاحات ویژهٔ نصب کامل

1. نصب‌یار قدیمی با نصب‌یار قفل‌شونده و دارای CSRF/کلید مالک هاست جایگزین شد؛ حساب و رمز پیش‌فرض توزیع نمی‌شود.
2. داده‌ها و تنظیمات مدرسهٔ نمونه از SQL حذف شدند، بدون حذف INSERTهای داخل بدنهٔ triggerهای همگام‌سازی.
3. خطای واقعی شاخص `student_discipline_records` در اسکیمای قدیمی MySQL اصلاح شد: شاخص اکنون به ستون موجود `created_at_jalali` اشاره می‌کند.
4. اتصال خراب/تنظیمات ناقص دیتابیس دیگر به دیتابیس یا مشخصات اتصال حدسی تبدیل نمی‌شود.
5. هر دو API همگام‌سازی کلید همان نصب را از فایل محرمانهٔ مشترک می‌خوانند؛ کلید قدیمی جاسازی‌شده در بسته‌ها وجود ندارد. endpoint اصلی نیز بر مبنای آخرین وصلهٔ 4.101، شامل آرشیو طراحی و انتخاب صفحات PDF، حفظ شده است.
6. کتابخانهٔ محلی جاافتادهٔ Chart.js و موتور PDF/فونت مربوط به TCPDF اضافه شدند؛ فایل Python تحلیل PDF نیز در هر دو بسته موجود است.
7. پوشه‌های خصوصی محافظت می‌شوند؛ heartbeat همگام‌سازی محلی فقط در نسخهٔ دسکتاپ اجرا می‌شود، نه صفحهٔ دریافت کلید سایت.
8. تست‌های قدیمی از وابستگی به شناسهٔ ثابت دانش‌آموز نمونه، شمارش دادهٔ نمونه برای صفحه‌بندی و کلید تاریخی ثابت جدا شدند؛ بررسی رفتاری حذف یا ضعیف نشده است.

## محدودیت‌ها و آنچه ادعا نمی‌شود

- محیط اجرا Linux است؛ **EXE روی Windows بومی اجرا نشده** و باز/بسته‌شدن پنجرهٔ Edge/Chrome، SmartScreen، چاپگر فیزیکی و دوربین گوشی در این نوبت آزمون نشده‌اند.
- تست MySQL با 5.7.29 انجام شد، نه MySQL 8.x. این نسخهٔ MySQL منقضی است و فقط ابزار آزمایش سازگاری بود؛ روی هاست تازه نسخهٔ پشتیبانی‌شده به کار ببرید و نصب را در محیط آزمایشی خودتان کنترل کنید.
- PHP داخلی ویندوز همان 8.1.34 بستهٔ مبناست و شاخهٔ منقضی است؛ فقط برای استفادهٔ محلی مورد اعتماد، نه میزبانی عمومی. تغییر عمدهٔ runtime بدون آزمون ویندوز انجام نشده است.
- قواعد Apache/Nginx باید در هاست مقصد اجرا/کنترل شوند؛ بررسی واقعی HTTP در این محیط برای Apache/Nginx انجام نشده است. قواعد روتر PHP دسکتاپ در آزمون اجرا شدند.
- ابزارهای اختیاری تحلیل/تبدیل PDF منبع، سرویس‌های پیامک/ربات و مجوزهای دوربین وابستگی‌های خود را دارند؛ توضیح در README-FA.md است. خروجی کارنامهٔ PDF با TCPDF همراه، واقعاً آزمون شد.

## بازتولید

```sh
python3 scripts/build-full-release.py
SITE=.cache/release-v1/package/site bash scripts/run-tests.sh
SITE=.cache/release-v1/package/SchoolDeskPro/www bash scripts/run-tests.sh
node tests/test-full-release-install.mjs
# فقط با MySQL آزمایشی محلی و رمز آزمایشی در فایل ignored:
RELEASE_TEST_MYSQL=1 node tests/test-full-release-install.mjs
PYTHONDONTWRITEBYTECODE=1 python3 tests/test-full-release-packaging.py
```

محدوده و شیوهٔ ساخت در `release-v1.0/BUILD.md`، راهنمای نصب در `release-v1.0/README-FA.md` و شناسهٔ فایل‌ها در `Release_V1.0-SHA256SUMS.txt` و manifestهای داخل ZIP است.
