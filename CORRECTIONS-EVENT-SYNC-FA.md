# اصلاحی «حالت فقط رویداد» برای همگام‌سازی دسکتاپ

تاریخ: ۱۸ شهریور ۱۴۰۵ · بر پایهٔ بسته‌های **bot-outbox** و **optimized-sync** ·
دو فایل دسکتاپ:
- `SchoolDeskPro/reports/includes/desk_sync.php` (موتور)
- `SchoolDeskPro/reports/desk-sync.php` (صفحهٔ تنظیمات)

## چه چیزی اضافه شد؟

در صفحهٔ **«همگام‌سازی با سایت»** یک گزینهٔ جدید: **«حالت ترافیک با سایت»** با دو حالت:

| حالت | رفتار | ترافیک روزانه (هر سیستم) |
|---|---|---|
| **کامل (پیش‌فرض)** | همان بهینه‌سازی قبلی: بررسی خالی هر ۳۰ ثانیه + چرخهٔ امنیتی هر ۵ دقیقه + push فوری هر تغییر | ~۹۰۰–۱,۰۵۰ |
| **فقط رویداد (کم‌ترافیک‌ترین)** | بدون هیچ بررسی دوره‌ای؛ درخواست همگام‌سازی **فقط** با ثبت/ویرایش/حذف واقعی + یک چک آشتی هر ساعت | **~۵۰–۱۰۰** |

در حالت «فقط رویداد»:
- هر ثبت/ویرایش/حذف در دسکتاپ → **همان‌همان فوری** به سایت (بدون تغییر، <۱ ثانیه).
- هیچ ping خالی و هیچ چرخهٔ دور وجود ندارد.
- فقط یک «چک آشتی» هر ساعت (مقایسهٔ کامل جداول با سایت) انجام می‌شود تا دو بانک همیشه همسان بمانند.
- **تأخیر واحد:** تغییری که روی **وب‌سایت** زده شود، در دسکتاپ **تا ۱ ساعت بعد** — یا **بلافاصله با اولین ویرایش بعدی در دسکتاپ** (هر چرخهٔ کامل دوطرفه است) — اعمال می‌شود.
- صف اعلان‌ها: دقیقاً مثل قبل؛ ارسال اعلان‌های دسکتاپ با همان چرخهٔ فوریِ ثبت داده انجام می‌شود.

پیش‌فرض «کامل» است؛ یعنی اگر گزینه را لمس نکنید، هیچ رفتاری عوض نمی‌شود.

## نصب

1. بستهٔ `optimized-sync` باید نصب باشد (اگر هنوز نصب نکرده‌اید، اول آن را نصب کنید).
2. فایل `SchoolDeskPro-FIX-v2.83.0-event-sync.zip` را در همان سطح `SchoolDeskPro` استخراج کنید (دو فایل جایگزین می‌شوند).
3. **برنامه را کامل ببندید و دوباره باز کنید** (daemon موتور قدیمی را تا پایان عمرش در حافظه دارد؛ بدون ری‌استارت، گزینهٔ جدید در صفحه نمایش داده می‌شود اما اثر نمی‌کند).
4. وارد «همگام‌سازی با سایت» شوید → **حالت ترافیک با سایت: فقط رویداد** → ذخیره.
5. برای دیدن اثر: در چند ساعت کاری، عدد «مجموع ارسال‌شده» فقط باید با ثبت‌های واقعی شما زیاد شود؛ در زمان بیکاری هیچ همگام‌سازی رخ نمی‌دهد (اشاره‌گر بالای صفحه فقط هر ساعت یک‌بار می‌چرخد).

## بازگشت

- سریع: در همان صفحه، حالت «کامل» را برگردانید و ذخیره کنید (اثر پس از باز شدن بعدی برنامه).
- کامل: دو فایل را از بستهٔ `optimized-sync` بازگردانید و daemon را ری‌استارت کنید.

## حدومرزها و شفافیت

- آزمایش: ۴۷ بررسی رفتار (شامل ۹ بررسی جدید حالت رویداد + رابط تنظیمات) + ۴ بررسی قرارداد throttle در سوئیت fast-UI + ۳۴/۴۵ بررسی صف ربات + **۳۱/۳۱ + ۳۱/۳۱** سوئیت رگرسیون + ۱۷۴ بررسی بستهٔ قبلی + بررسی‌های بستهٔ جدید. همه روی PHP 8.3 (WASM) + SQLite با شبکهٔ شبیه‌سازی‌شده؛ **اندازه‌گیری زنده روی هاست/ویندوز انجام نشده**.
- «چک آشتی ساعتی» عمداً حذف نشده: بدون آن، اگر روزی یک‌بار همگامی کامل انجام نشود، دو بانک می‌توانند به‌تدریج از هم بربیفザینند؛ این چک ارزان است (هر ساعت چند درخواست).
- اگر از وب‌سایت برای **تغییرات روزمره** (نه فقط مشاهده/گزارش) استفاده می‌کنید، حالت «کامل» مناسب‌تر است.

## فایل‌ها و امضا

- بسته: `SchoolDeskPro-FIX-v2.83.0-event-sync.zip` (دو فایل)
- امضا: `EVENT-SYNC-SHA256SUMS.txt`
- شواهد: `docs/event-sync/packages.json` و `docs/event-sync/verification.json`
- آزمون‌ها: `tests/test-optimized-sync.mjs` (بخش‌های ۹–۱۳) و
  `tests/test-event-sync-packaging.py`
