==========================================================
  به‌روزرسانی v4.57.0 — رفع خطای jalali_now() در اسکن
==========================================================

خطای گزارش‌شده هنگام اسکن تگ:
  «خطای سرور: Call to undefined function jalali_now()»

علت:
  تابع jalali_now() در فایل includes/exams_helper.php پروژه
  تعریف شده است، اما API اسکنر (attendance-scan-api.php) آن
  فایل را بارگذاری نمی‌کند. توابع حضور و غیاب (ثبت حضور/تأخیر/
  غیبت و ساخت تگ) از jalali_now() استفاده می‌کردند و چون در
  مسیر اسکن تعریف نشده بود، ثبت اسکن با خطای مهلک متوقف می‌شد.

  (نکته: به لطف اصلاح v4.56.0 این خطا به‌جای «خطای شبکه» به
  صورت پیام شفاف JSON به اسکنر رسید و قابل تشخیص شد.)

راه‌حل:
  تعریف امن jalali_now() (با همان فرمت اصلی 'Y/m/d H:i' و
  محافظ function_exists) مستقیماً داخل
  includes/attendance_helpers.php اضافه شد؛ بنابراین هر جایی
  که این هلپر بارگذاری شود — از جمله API اسکنر، صفحه حضور و
  غیاب و کرون — تابع همیشه در دسترس است و اگر exams_helper.php
  قبلاً آن را تعریف کرده باشد تداخلی رخ نمی‌دهد.

فایل تغییر یافته (۱ فایل):
  1) includes/attendance_helpers.php

----------------------------------------------------------
نصب:
----------------------------------------------------------
۱) فقط فایل includes/attendance_helpers.php را جایگزین کنید.
   (این نسخه شامل همه اصلاحات v4.56.0 — صف تعویقی اطلاع‌رسانی —
   هم هست.)
۲) نیازی به تغییر دیتابیس نیست.

----------------------------------------------------------
تست پیشنهادی:
----------------------------------------------------------
- با گوشی یک تگ را اسکن کنید: باید «حضور ثبت شد» یا «تأخیر»
  بیاید (نه خطای سرور).
- اسکن دوباره همان تگ: «قبلاً ثبت شده» (تکراری).
- بخش «تگ جدید» در صفحه تگ‌ها هم باید بدون خطا کار کند
  (آن هم از jalali_now استفاده می‌کند).
