# اصلاح محدودهٔ دکمهٔ بازگشت

بازگشت عمومیِ اضافه‌شده در اصلاح قبلی لغو شد. **صفحات عادی دیگر نوار «بازگشت / صفحهٔ اصلی» یا کنترل بستنِ اضافه‌شده توسط آن لایه را ندارند.** دکمه‌های اصلی و قدیمی هر صفحه، مانند انصراف/بازگشت پایین فرم، حفظ شده‌اند. لینک اضافهٔ بالای فرم دانش‌آموز و لینک‌های تازهٔ صفحات خطا/ابزارهای ناموجود نیز حذف شدند.

## فقط این پیش‌نمایش‌ها

- پیش‌نمایش چاپ کارنامه و چاپ گروهی؛
- پیش‌نمایش HTML برای چاپ/ذخیرهٔ PDF، از جمله گزارش دانش‌آموزان و فهرست کلاسی؛
- نمای مستقلِ پیش‌نمایش چاپ کارت ورود و تگ QR؛ صفحهٔ مدیریت/تنظیمات آن‌ها نوار عمومی نمی‌گیرد؛
- ویرایشگر زندهٔ آزمون: **یک دکمهٔ واقعی داخل `mainToolbar`، با همان قواعد ظاهری دکمه‌های ویرایشگر**، نه نوار جداگانه بالای صفحه.

نمای مستقل چاپ تگ/کارت و فهرست کلاسی نیز از نوار ابزار موجود خود استفاده می‌کند. این کنترل‌ها در چاپ و PDF خروجی دیده نمی‌شوند. به خود فایل باینری PDF هیچ دکمه یا محتوایی تزریق نشده است.

محدوده از علامت صریح روی بدنهٔ سندِ پیش‌نمایش تعیین می‌شود؛ صرف نام مسیر، وجود history یا بازشدن در پنجرهٔ جدید باعث افزودن دکمه نمی‌شود. شناسهٔ کش صفحهٔ مشترک و پیش‌نمایش‌های مستقل تغییر کرده تا کد قبلی در مرورگر باقی نماند.

## دانلود و نصب

- [اصلاحی سایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-preview-navigation.zip)
- [اصلاحی دسکتاپ](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-preview-navigation.zip)
- [SHA256](PREVIEW-NAVIGATION-SHA256SUMS.txt) · [۱۶ مسیر payload](scripts/preview-navigation-files.json)

هر دو بسته فقط **۱۶ فایل وبِ تغییرکرده** دارند، حدود ۱۲۹ کیبی‌بایت، و باید **پس از آخرین بستهٔ student-workflow** نصب شوند. نصب مستقل یا جایگزین مجموعهٔ اصلاحات قبلی نیستند.

ابتدا پشتیبان بگیرید. سایت: محتویات `site-update-v4.152.0` را در ریشهٔ نصب موجود ادغام کنید. دسکتاپ: برنامه را ببندید و `SchoolDeskPro/www` را با `www` موجود ادغام کنید. در صورت نیاز OPcache را بازنشانی و مرورگر را نوسازی کنید.

هیچ ZIP قبلی، نسخهٔ کامل Release_V1.0، فایل اجرایی دسکتاپ، داده، تنظیم اتصال، قلم یا تصویر بارگذاری‌شده تغییر نکرده است. فرم و سریال، انتخاب ستون‌های گزارش، انتظار برای قلم چاپ، مرکز هدر و سیاست اندازهٔ پنجرهٔ دسکتاپ از اصلاح قبلی حفظ شده‌اند.

## بررسی انجام‌شده

- ۳۹۰ حالت چیدمان: **۳۵۵ حالت عادی بدون کنترل اضافه** و **۳۵ حالت پیش‌نمایش مجاز با کنترل بازگشت**؛ پنج حالتِ ویرایشگر، دکمه را داخل نوار ابزار خود داشتند.
- ۷۹ بررسی متمرکز مرورگری: محدودهٔ نمایش، عدم ایجاد تکراری، تطابق font/padding/border/color دکمهٔ ویرایشگر با دکمهٔ مجاور، عملکرد بازگشت و مخفی‌شدن کنترل در رسانهٔ چاپ.
- بدون خروج سند از عرض یا هم‌پوشانی فوتر؛ نمونه‌های Axe بدون تخلف گزارش‌شده.
- هر ۳۰ سوئیت بازگشت سایت موفق؛ ۳۰ بررسی اختصاصی فرم/گزارش نیز موفق.
- بررسی چاپ واقعی Chromium برای قلم، کپچا و فهرست دبیر: دو سرستون و دقیقاً دو صفحهٔ A4 حفظ شدند.
- چهار آزمون بسته‌بندی و بررسی نحو ES5 موفق.

[تصویر نوار ابزار ویرایشگر](docs/preview-navigation/editor-toolbar.png) · [دادهٔ بررسی محدوده](docs/preview-navigation/audit.json)

آزمون صفحات با دادهٔ ساختگی در PHP/SQLite جداگانه و Chromium انجام شده است؛ درخواست‌های AJAX در ممیزی چیدمان شبیه‌سازی می‌شوند. این نتیجه گواهی AAA یا آزمون چاپگر فیزیکی نیست.

ساخت فقط اصلاحی‌های جدید: `python3 scripts/release-preview-navigation.py`؛ بررسی بسته‌ها: `python3 tests/test-preview-navigation-packaging.py`. آزمون مرورگر `tests/test-preview-navigation.mjs` از کاتالوگ تولیدشده توسط `tests/test-ui-page-catalog.mjs` استفاده می‌کند.
