# اصلاحی بهینه‌سازی همگام‌سازی دسکتاپ (بدون تغییر کارکرد)

تاریخ: ۱۸ شهریور ۱۴۰۵ · بر پایهٔ بستهٔ **bot-outbox** · تنها **یک فایل** دسکتاپ:
`SchoolDeskPro/reports/includes/desk_sync.php`

## چرا این اصلاحی؟

سرعت درخواست‌های نرم‌افزار دسکتاپ به سایت اندازه‌گیری شد. در حالت عادی (اینترنت
دائماً وصل، دو سیستم، ۵ ساعت در روز) هر سیستم حدود **۵,۰۰۰–۵,۵۰۰ درخواست در روز**
به سایت می‌فرستاد، در حالی‌که دادهٔ واقعی آن مدرسه (مثلاً ۲۰ مورد انضباطی + نوبندی +
اعلان‌ها) فقط چندین ده درخواست در روز است. بخش عمدهٔ درخواست‌ها «نگهداری اتصال» بود:

| لایهٔ پیشین | فاصله | هزینهٔ روزانهٔ هر سیستم |
|---|---|---|
| بررسی خالی «سایت تغییری دارد؟» (ping) | هر ~۵ ثانیه | ~۳,۴۵۰ درخواست |
| تحویل صف اعلان به سایت — **حتی با صف خالی** | هر ۱۵ ثانیه | ~۱,۲۰۰ درخواست |
| چرخهٔ کامل امنیتی (handshake + pull + فایل‌ها) | هر ۲ دقیقه | ~۴۵۰–۶۰۰ درخواست |
| ثبت تغییرات واقعی | فوری | ده‌ها درخواست (کار واقعی) |

## سه تغییر انجام‌شده (همه در یک فایل)

1. **صف خالی دیگر درخواست نمی‌فرستد.** تحویل اعلان به سایت فقط زمانی HTTP
   می‌زند که واقعاً کاری برای تحویل باشد. صف خالی حالت عادی است و ۱,۲۰۰
   درخواست بی‌محتوای روزانه حذف می‌شود. **هیچ اثری روی کارکرد ندارد.**
2. **بررسی خالی سایت از هر ~۵ ثانیه به هر ۳۰ ثانیه.** تغییری که روی **دسکتاپ**
   زده شود، **همان‌همان فوری** به سایت می‌رود (بدون تغییر). تنها تأخیر:
   تغییری که روی **وب‌سایت** زده شود، تا ~۳۰ ثانیه بعد در دسکتاپ دیده می‌شود
   (پیش‌تر ~۵ ثانیه).
3. **چرخهٔ کامل امنیتی از هر ۲ دقیقه به هر ۵ دقیقه** — فقط وقتی **هیچ** تغییری در
   هیچ‌کدام از دو طرف نباشد. هر لحظه که تغییری وجود داشته باشد، چرخهٔ کامل فوری
   اجرا می‌شود (دست‌نخورده).

به‌علاوه، یک ثبت‌زمان داخلی («آخرین تأیید همگامی») حفظ می‌شود تا تحویل اعلان‌ها
در همان ریتم ۱۵ ثانیه‌ای قبلی بماند؛ یعنی ارسال اعلان‌های صف‌دار عملاً تأخیر
جدیدی نمی‌گیرد.

**نتیجهٔ کلان:** از ~۱۰,۵۰۰ درخواست در روز (دو سیستم) به حدود **۱,۵۰۰–۲,۵۰۰**؛
یعنی کاهش ~۸۰–۸۵٪ ترافیک پس‌زمینه، درحالی‌که:

- ثبت هر تغییر در دسکتاپ → ارسال **فوری** به سایت (بدون تغییر)
- ذخیرهٔ اعلان‌ها هنگام قطعی و ارسال خودکار پس از اتصال (بدون تغییر)
- ترازنامهٔ تلاش و خطا و رسید تحویل (بدون تغییر)
- تمام خطوط امنیتی HTTPS و کلید خصوصی (بدون تغییر)
- daemon دسکتاپ (فایل `desk-sync-daemon.php`) **کلاً دست‌نخورده**

## نصب

1. بستهٔ `bot-outbox` باید قبلاً نصب شده باشد (این اصلاحی بعد از آن است).
2. فایل `SchoolDeskPro-FIX-v2.83.0-optimized-sync.zip` را در همان سطحی که
   `SchoolDeskPro` قرار دارد استخراج کنید؛ فقط یک فایل بازمی‌گردد:
   `SchoolDeskPro/reports/includes/desk_sync.php` — جایگزین فایل فعلی.
3. **نکتهٔ مهم:** فرآیند همگام‌سازی پس‌زمینه (daemon) کد قدیمی را تا پایان
   عمرش در حافظه دارد. پس از جایگزینی، برنامه را کاملاً ببندید و دوباره باز
   کنید (اگر برنامه ساعت‌ها باز بوده و daemon از کارهای قبلی زنده است،
   یک‌بار ری‌استارت ویندوز مطمئن‌ترین راه است).
4. در پنل «همگام‌سازی با سایت» وضعیت را ببینید؛ بعد از چند دقیقهٔ آرام،
   «آخرین اجرای موفق» هر ~۵ دقیقه به‌روز شود (نه هر ۲ دقیقه) — نشانهٔ کارکردن
   حالت جدید است.

## بازگشت (در صورت نیاز)

فایل `SchoolDeskPro/reports/includes/desk_sync.php` از بستهٔ `bot-outbox`
(`SchoolDeskPro-FIX-v2.83.0-bot-outbox.zip`) دوباره جایگزین شود و daemon
ری‌استارت شود. هیچ داده‌ای حذف یا تغییر نمی‌کند.

## حد‌ومرزها و شفافیت

- آزمایش‌ها روی PHP 8.3 (WASM) + SQLite با ارائه‌دهندهٔ ربات و شبکهٔ واقعی
  شبیه‌سازی‌شده انجام شده: ۲۸ بررسی جدید رفتار + همهٔ سوئیت‌های پیشین دسکتاپ و
  سایت + آزمون‌های صف ربات. **اندازه‌گیری زندهٔ مصرف روی هاست واقعی و ویندوز
  انجام نشده است.**
- کاهش ۸۰–۸۵٪ تخمینٔ دقیق بر اساس نرخ‌های ثابتِ کد (۳۰ ثانیه / ۵ دقیقه / ۱۵
  ثانیه) است؛ اعداد واقعی با حجم فایل‌های آزمون و عکس دانش‌آموزان کمی نوسان
  دارد (این بخش‌ها فقط هنگام وجود تغییر کار می‌کنند).
- این اصلاحی **ترافیک دسکتاپ** را کم می‌کند؛ بار دائمی دریافت تلگرام
  (`telegram-poll.php` روی سایت) موضوع جداگانه است و با آن دست‌نخورده است.
  همچنین تأیید قوانین هاست‌دار (پارس‌پک) دربارهٔ استفادهٔ ربات به‌دنبال پاسخ
  کتبی پشتیبانی است.

## فایل‌ها و امضا

- بسته: `SchoolDeskPro-FIX-v2.83.0-optimized-sync.zip` (یک فایل)
- امضا: `OPTIMIZED-SYNC-SHA256SUMS.txt`
- شواهد: `docs/optimized-sync/packages.json` و `docs/optimized-sync/verification.json`
- آزمون‌ها: `tests/test-optimized-sync.mjs` (رفتار)،
  `tests/test-optimized-sync-packaging.py` (بسته/امنیت)
