# اصلاح اندازهٔ بخش‌های اصلی پنج مرکز مدیریت در موبایل

**بستهٔ اصلاحی پس از fast-ui؛ بدون بازسازی Release_V1.0، تغییر فایل اجرایی یا افزایش شمارهٔ نسخه.**

## دانلود و نصب

- [اصلاحی سایت — ۱۲ فایل، حدود ۲۲ کیلوبایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-mobile-hubs.zip)
- [اصلاحی دسکتاپ — همان ۱۲ فایل وب، حدود ۲۲ کیلوبایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-mobile-hubs.zip)
- [هش SHA256](MOBILE-HUBS-SHA256SUMS.txt) · [فهرست دقیق فایل‌ها](scripts/mobile-hubs-files.json)

ترتیب نصب از مبنای موجود:

`Release_V1.0 → svg-responsive → student-workflow → preview-navigation → fast-ui → mobile-hubs`

این اصلاحی جایگزین بستهٔ کامل یا اصلاحات قبلی نیست.

۱. از نصب و داده‌ها پشتیبان بگیرید.
۲. سایت: محتویات `site-update-v4.152.0` را در ریشهٔ نصب موجود **ادغام** و فایل‌های هم‌نام را جایگزین کنید؛ خود پوشهٔ نصب را حذف نکنید.
۳. دسکتاپ: برنامه را کاملاً ببندید؛ محتویات `SchoolDeskPro/www` را با پوشهٔ `www` نصب ادغام و سپس برنامه را اجرا کنید. فایل exe عوض نمی‌شود.
۴. در هاست دارای OPcache بدون بررسی زمان تغییر فایل، کش PHP را بازنشانی کنید. منابع تازه کلید کش دارند؛ در صورت باقی‌ماندن ظاهر قبلی نوسازی کامل انجام دهید.

داده‌ها، تنظیمات اتصال، رمزها، uploads، قلم‌ها، پایگاه‌داده و فایل‌های اجرایی در این بسته نیستند. همهٔ ZIPهای قبلی، از جمله دو بستهٔ کامل، با مقایسهٔ هش با نسخهٔ منتشرشده کنترل شده‌اند و تغییر نکرده‌اند.

## علت اصلی و اصلاح انجام‌شده

در آزمون نسخهٔ مبنا با عرض ۳۹۰ پیکسل، ارتفاع iframe در **هر ۱۷ تب فقط ۱۵۰ پیکسل** بود؛ بنابراین مسئله صرفاً بیرون‌زدگی افقی صفحه نبود. قانون قدیمی `.card form.grid` نیز ارتفاع فرم را به `38vh` محدود و درون آن اسکرول ایجاد می‌کرد؛ در چنین قابی این سقف فقط حدود ۵۷ پیکسل بود.

اصلاحات:

- در عرض حداکثر ۹۰۰ پیکسل، ارتفاع قاب با محتوای واقعی تغییر می‌کند؛ حداقل ۲۴۰ و سقف احتیاطی ۳۰٬۰۰۰ پیکسل دارد. بالاتر از سقف، محتوای اضافی حذف نمی‌شود و اسکرول داخلی باقی می‌ماند.
- محدودیت ارتفاع و اسکرول تو‌در‌توی فرم‌های شبکه‌ای حذف شده و حداقل ارتفاع مبتنی بر viewport داخلی خنثی شده است.
- کارت‌ها و فرم‌ها در موبایل یک‌ستونه و با حاشیه‌های کمتر نمایش داده می‌شوند. ورودی‌های آزموده‌شده حداقل ارتفاع ۴۴ پیکسل دارند؛ جدول‌های عریض به‌صورت محلی اسکرول می‌شوند، نه با کوچک‌کردن کل صفحه یا حذف ستون‌ها.
- تب‌ها در موبایل دو ستون دارند؛ تب آخرِ تنها تمام عرض را می‌گیرد. یک قاب مشترک، عنوان مشخص، حالت انتخاب‌شده، کلیدهای راست/چپ متناسب با RTL و تاریخچهٔ مرورگر حفظ شده‌اند.
- ارتفاع پس از تغییر محتوا، قلم، اندازهٔ پنجره و زیرتب بازاندازه‌گیری می‌شود؛ حلقهٔ polling جدیدی اضافه نشده است.
- پنجره‌های داخلی مانند قفل دسته‌جمعی کارنامه و بازیابی پشتیبان در محدودهٔ قابل‌دید صفحهٔ والد باز می‌شوند، نه وسط یک iframe بسیار بلند.
- تغییر مسیر داخلی حتی با حذف `embedded=1`، قاب را به صفحهٔ کامل با هدر و فوتر تکراری تبدیل نمی‌کند. پایش اتصال دسکتاپ نیز داخل iframe دوباره آغاز نمی‌شود.
- در نبود JavaScript، پیوندهای معمول تب و پیوند بازکردن مستقیم بخش باقی می‌مانند؛ ارتفاع خودکار به JavaScript نیاز دارد.

**دقت تشخیص:** در اندازه‌گیری مبنا، کارت‌ها از نظر نسبت عرض به شبکه فشرده نبودند و CSS قبلی از قبل `col-span-2/3` را در موبایل بازنشانی می‌کرد. بازنشانی عمومی span در اصلاحی یک محافظ تکمیلی است، نه علت اصلی اثبات‌شدهٔ مشکل.

در نمایش عریض، قاب با ارتفاع متناسب با پنجره و اسکرول داخلی باقی می‌ماند؛ فرم‌ها به چیدمان یک‌ستونهٔ اجباری موبایل تبدیل نمی‌شوند. قواعد جدید اندازه‌گذاری به محیط نمایش محدودند؛ فایل‌های خروجی Word/PDF و منطق چاپ، ورود، مجوز، CSRF و عملیات داده تغییر نکرده‌اند. دکمهٔ بازگشت عمومی به صفحه‌ها اضافه نشده است.

## پوشش تب‌ها و محدودیت سه ماژول

| مرکز | تب‌های تحت پوشش قاب مشترک |
|---|---|
| مدیریت دروس | کلاس‌ها و دروس، دبیران، برنامه هفتگی، سال تحصیلی |
| مدیریت کارنامه‌ها | کارنامه‌ها، ایمپورت Wizard، تحلیل و مقایسه، مدیریت ثبت نمره، اعلام سراسری |
| بازیابی دانش‌آموز | ایمپورت دانش‌آموزان، آپلود تصاویر ZIP |
| پیامک و اعلان‌ها | اعلان‌ها، سامانه پیامکی |
| تنظیمات دیگر | لاگ فعالیت، مایگریشن، پشتیبان‌گیری، مستندات API |

**محدودیت مهم:** فایل‌های `import.php`، `grade-entry-management.php` و `import-photos.php` در بستهٔ مبنای موجود در مخزن نیستند. در تست، این سه مسیر صادقانه پاسخ «در نسخهٔ مبنا موجود نیست» با وضعیت 404 دارند. بنابراین محتوای واقعی **۱۴ تب موجود** بررسی شده و برای سه تب دیگر فقط قاب، ناوبری و نمایش پاسخ نبود ماژول آزموده شده است. هیچ فایل ساختگی برای جایگزینی این سه ماژول در اصلاحی قرار ندارد؛ اگر در نصب شما موجود باشند، این بسته آن‌ها را حذف یا بازنویسی نمی‌کند. تأیید عملیات و چیدمان اختصاصی آن سه ماژول نیازمند سورس واقعی آن‌هاست.

## آزمون و شواهد

- **۳۱ مجموعه‌آزمون قبلی برای سایت و ۳۱ مجموعه برای دسکتاپ: همگی موفق**؛ شامل ورود، فونت، فوتر و اتصال، کارت، چاپ، Word، آزمون کلاسی و اسکنر در محدودهٔ harness موجود.
- تولید HTML واقعی با PHP و دادهٔ آزمایشی: پنج والد، ۱۷ مسیر تب و چهار نمای داخلی؛ انتخاب اولیهٔ سمت سرور، پارامتر نامعتبر/آرایه و جلوگیری از نمایش قاب به مهمان بررسی شد. آزمون ۵۳ سناریوی موجود گروه آزمون کلاسی نیز در آماده‌سازی داده‌ها موفق بود.
- **سایت: ۶۸۵ بررسی مرورگر**؛ ۱۰۲ رکورد چیدمان شامل ۱۷ مبنا در عرض ۳۹۰ و ۸۵ پس از اصلاح در عرض‌های ۳۲۰، ۳۹۰، ۷۶۸، ۱۰۲۴ و ۱۴۴۰.
- **دسکتاپ: ۲۷۹ بررسی مرورگر**؛ ۳۴ رکورد چیدمان در عرض‌های ۳۹۰ و ۱۴۴۰ روی فایل‌های وب توزیع دسکتاپ.
- چهار نمای داخلی شامل ماتریس هفتگی کلاس، ایمپورت CSV دبیر، ویرایش دبیر و ایجاد کارنامه، هرکدام در سه عرض موبایل/تبلت و در هر دو توزیع آزموده شدند؛ ۱۲ چیدمان اضافه برای هر توزیع.
- رشد و جمع‌شدن ۷۰۰ پیکسلی محتوا، نبود حلقهٔ ارتفاع، تاریخچهٔ بازگشت، صفحه‌کلید RTL، انتخاب لمسی و تغییر viewport به افقی، نبود JavaScript، تغییر مسیر بدون پرچم embedded و دیده‌شدن دو پنجرهٔ داخلی موفق بودند. پنجرهٔ بازیابی فقط باز و اندازه‌گیری شد؛ عملیات بازیابی پایگاه‌داده اجرا نشد.
- خطای JavaScript در صفحهٔ تحت پایش ثبت نشد. نمونه‌برداری Axe در صفحهٔ کارنامه‌ها، پس از بستن پنجره، در هر دو توزیع تخلفی گزارش نکرد؛ این نمونه‌برداری گواهی دسترس‌پذیری همهٔ صفحه‌ها یا استاندارد AAA نیست.
- پنج آزمون بسته‌بندی جدید و پنج آزمون بسته‌بندی fast-ui موفق: تعداد و بایت دقیق فایل‌ها، هش، عدم تغییر آرشیوهای قدیمی و حفظ گیت مجوز والدها.

مرورگر آزمون Chromium است و HTML خروجی PHP در iframe واقعی و هم‌مبدأ بارگذاری شده؛ پاسخ APIهای پس‌زمینه شبیه‌سازی شده است. این آزمایش جای آزمون روی تلفن فیزیکی، Windows/WebView نصب‌شده، شبکهٔ مدرسه یا دادهٔ واقعی شما نیست و ادعای تأیید ارسال پیامک، ایمپورت فایل یا بازیابی واقعی ندارد.

### گزارش‌ها و تصاویر

- [اندازه‌گیری سایت و مبنا](docs/mobile-hubs/layouts.json) · [اندازه‌گیری توزیع دسکتاپ](docs/mobile-hubs/layouts-desktop.json)
- [Axe سایت](docs/mobile-hubs/accessibility.json) · [Axe دسکتاپ](docs/mobile-hubs/accessibility-desktop.json)
- دبیران: [قبل](docs/mobile-hubs/before-courses-management-teachers.png) / [بعد](docs/mobile-hubs/after-courses-management-teachers.png)
- [سامانه پیامکی بعد از اصلاح](docs/mobile-hubs/after-messages-management-sms.png)
- [مستندات API بعد از اصلاح](docs/mobile-hubs/after-other-settings-api.png)
- [ماتریس برنامهٔ کلاس](docs/mobile-hubs/after-class-matrix.png)

## بازتولید آزمون‌ها

```sh
python3 scripts/stage-mobile-hubs.py --before
python3 scripts/stage-mobile-hubs.py
python3 scripts/stage-mobile-hubs.py --desktop
SITE=.cache/mobile-hubs/before HUB_BEFORE=1 node tests/test-mobile-hubs-fixtures.mjs
SITE=.cache/mobile-hubs/site node tests/test-mobile-hubs-fixtures.mjs
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www HUB_DESKTOP=1 node tests/test-mobile-hubs-fixtures.mjs
node tests/test-mobile-hubs-browser.mjs
HUB_DESKTOP=1 HUB_AFTER_ONLY=1 HUB_QUICK=1 node tests/test-mobile-hubs-browser.mjs
SITE=.cache/mobile-hubs/site bash scripts/run-tests.sh
SITE=.cache/mobile-hubs/desktop/SchoolDeskPro/www bash scripts/run-tests.sh
python3 scripts/release-mobile-hubs.py
python3 tests/test-mobile-hubs-packaging.py
python3 tests/test-fast-ui-packaging.py
```

آزمون مرورگر به Playwright، @sparticuz/chromium و @axe-core/playwright در `.cache/browser/node_modules` و کتابخانه‌های سیستم Chromium نیاز دارد. در sandbox این اجرا، `LD_LIBRARY_PATH=$PWD/.cache/browser/lib/lib` نیز تنظیم شد. builder جدید فقط دو ZIP مربوط به mobile-hubs را می‌سازد؛ builderهای تاریخی را برای انتشار این اصلاحی اجرا نکنید.
