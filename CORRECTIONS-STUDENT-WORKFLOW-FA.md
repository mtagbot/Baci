# اصلاح فرم دانش‌آموز، گزارش، بازگشت و پنجرهٔ دسکتاپ

## دانلود و نصب

- [اصلاحی سایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-student-workflow.zip) — ۲۵ فایل وب، حدود ۱۵۳ کیبی‌بایت.
- [اصلاحی دسکتاپ](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-student-workflow.zip) — همان ۲۵ فایل، به‌اضافهٔ `SchoolDeskPro.exe` بازسازی‌شده؛ حدود ۱۹۷ کیبی‌بایت.
- [SHA256](STUDENT-WORKFLOW-SHA256SUMS.txt) · [فهرست فایل‌های وب](scripts/student-workflow-files.json)

**این بسته‌ها روی آخرین نصب با اصلاحی SVG قبلی اعمال می‌شوند؛ نصب مستقل نیستند.** هیچ نسخهٔ کامل Release_V1.0 بازسازی نشده و آرشیوهای قبلی تغییر نکرده‌اند. نسخه‌های داخلی 4.152.0 / 2.83.0 ثابت‌اند.

1. از برنامه، دیتابیس و بارگذاری‌ها پشتیبان مستقل بگیرید. دسکتاپ را کاملاً ببندید.
2. سایت: محتویات `site-update-v4.152.0` را با ریشهٔ سایت موجود ادغام/جایگزین کنید.
3. دسکتاپ: محتویات `SchoolDeskPro` را با پوشهٔ موجود ادغام کنید؛ **هم فایل اجرایی و هم فایل‌های `www` باید جایگزین شوند**. پوشهٔ تو‌در‌تو نسازید.
4. OPcache را در صورت نیاز بازنشانی کنید و مرورگر را نوسازی کنید. شناسهٔ کش دارایی‌های مشترک و صفحات مستقل چاپ نیز تغییر کرده است.
5. یک ثبت/ویرایش آزمایشی، گزارش با قلم منتخب و Minimize/بازگردانی پنجره را در دستگاه خود بررسی کنید.

فایل اتصال، دیتابیس، قلم‌ها و تصاویر بارگذاری‌شده، نشست‌ها، اسکنر بازگشت و نصب‌کننده در بسته نیستند. مهاجرت جدید دیتابیس لازم نیست. برای بازگشت، فایل‌های جایگزین‌شده را از پشتیبان برگردانید؛ در دسکتاپ، فایل اجرایی قبلی را نیز برگردانید.

## ۱. فرم حرفه‌ای و ورود با کیبورد

فرم ثبت و ویرایش به پنج بخش با پیوند پرش تقسیم شده است: **هویت، تحصیل، والدین، تماس و نشانی، سلامت**. برچسب‌ها به ورودی‌ها متصل‌اند؛ ترتیب DOM و Tab مطابق ترتیب ورود است و `tabindex` مثبت استفاده نشده است.

- Tab و Shift+Tab برای حرکت عادی؛ Enter در ورودی متنی برای بعدی و Shift+Enter برای قبلی. Enter در textarea همچنان سطر جدید و در کنترل‌های انتخابی رفتار بومی دارد. Enter متنی باعث ذخیرهٔ ناخواسته نمی‌شود.
- انتقال خودکار هنگام تکمیل کد ملی، سریال، اجزای تاریخ، کد پستی و موبایل داخلی با قالب `09…`؛ شماره‌های بین‌المللی و تماس‌های با طول متغیر زودتر از موعد رها نمی‌شوند.
- پرش هنگام حذف، چسباندن یا ویرایش فیلدی که از ابتدا کامل بوده انجام نمی‌شود؛ ترکیب حروف توسط صفحه‌کلید نیز در میانه قطع نمی‌شود. انتقال خودکار را می‌توان خاموش کرد.
- ارقام فارسی/عربی بدون حذف صفر ابتدایی یکسان می‌شوند. خطای اعتبارسنجی سمت سرور، داده‌های فرم را نگه می‌دارد و مانع ذخیرهٔ ناقص می‌شود.
- کد دانش‌آموزی ورودی مستقل دارد؛ جنسیت و شرح محدودیت ورزش نیز قابل ثبت‌اند. نام کوچک مادر هنگام وجود نام خانوادگی دیگر به‌اشتباه خالی ذخیره نمی‌شود.

### سریال

سه ورودی جدا و پشت‌سرهم:

**`356750` → `ب` → `35`**

قالب‌های قدیمی مانند `ب/35/356750`، `35/356750/ب` و `356750/ب/35` و ارقام فارسی خوانده می‌شوند. قالب قدیمیِ صرفاً شش‌رقمی نیز پشتیبانی می‌شود. اگر سریال سه‌بخشی قبلاً فقط در ستون کد دانش‌آموزی بوده، برای نمایش سریال شناسایی می‌شود، بدون پاک‌کردن کد دانش‌آموزی.

ستون ذخیره‌سازی قبلی حفظ شده است؛ تغییرات هماهنگ با واردکننده‌ها و انتقال سال تحصیلی می‌ماند. مقدار شناخته‌شدهٔ دست‌نخورده عیناً حفظ می‌شود و سریال تازه در قالب سازگار قبلی ذخیره می‌شود. مقدار ناشناخته خودکار حدس زده یا حذف نمی‌شود: مقدار اصلی و هشدار نمایش داده می‌شود و تا واردکردن جایگزین معتبر محفوظ می‌ماند.

## ۲. گزارش انتخابی و قلم چاپ نهایی

پنجرهٔ «گزارش دانش‌آموزان انتخاب‌شده» اکنون **۵۳ ستون قابل انتخاب جداگانه** در هفت گروه دارد:

- هویت و سه جزء سریال؛
- والدین، تاریخ تولد، تحصیلات، شغل و سرپرستی؛
- تماس، کد پستی، نشانی و مسکن؛
- سلامت و پوشش؛
- تحصیل و وضعیت فعالیت؛
- کارنامه/معدل؛
- انضباط.

انتخاب همه/پاک‌کردن انتخاب‌ها، حرکت کیبورد داخل پنجره و بازگشت فوکوس پس از بستن اضافه شده است. ستون‌ها از فهرست مجاز ثابت می‌آیند؛ رمز عبور، توکن و اطلاعات داخلی ورود قابل دریافت نیستند. دسترسی و CSRF حفظ شده‌اند. نبود انتخاب، خطای قابل فهم با مسیر بازگشت می‌دهد.

**علت مسیر fallback چاپ اصلاح شد:** فراخوانی چاپ پیش از آماده‌شدن قلم حذف شده است. چاپ HTML/PDF ابتدا بارگذاری واقعی هر دو وزن معمولی و ضخیم قلم منتخب را بررسی و سپس چاپ را آغاز می‌کند. اگر بارگذاری شکست بخورد، چاپ خودکار متوقف و امکان تلاش دوباره نمایش داده می‌شود؛ فایل با قلم جایگزین بی‌صدا چاپ نمی‌شود.

گزارش پرستون روی صفحه پیمایش افقی محلی دارد. صفحه‌بندی چاپ با تعداد ستون‌ها A4 عمودی/افقی یا برای بیش از ۱۸ ستون A3 افقی انتخاب می‌شود؛ در گزارش بسیار عریض، انتخاب ستون‌های کمتر یا Excel خواناتر است. این گزارش محدود به یک صفحه نیست؛ قرارداد تک‌صفحه‌ایِ **فهرست کل مدرسه** جداست و تغییر نکرده است.

Excel و Word همچنان خروجی HTML سازگار هستند، نه XLSX/DOCX جدید. آن‌ها نام واقعی قلم را اعلام می‌کنند و برای نمایش یکسان به نصب همان قلم روی دستگاه بازکننده نیاز دارند. قلم PDF مرورگر در آزمون واقعاً جاسازی شده است.

## ۳. مرکز هدر

جایگاه کاشی‌ها از گروه تاریخ و حساب کاربری جدا شده و دو طرف هدر متقارن‌اند. در دسکتاپ عریض، کاشی‌ها دقیقاً وسط یک ردیف‌اند؛ در عرض دسکتاپ محدودتر، ردیف دومِ مرکزی می‌گیرند تا با نام مدرسه/تاریخ تداخل نکنند. مرکز هندسی در عرض‌های ۱۰۲۴، ۱۴۴۰ و ۱۹۲۰ اندازه‌گیری شده است.

## ۴. خروج از بن‌بست

لایهٔ مشترک، روی صفحات عادی و مستقلِ پیش‌نمایش/چاپ، پیوند بازگشت و صفحهٔ اصلی می‌گذارد. وقتی پنجرهٔ قبلیِ هم‌مبدأ در دسترس باشد، «بستن و بازگشت به پنجرهٔ قبلی» نیز اضافه می‌شود. بستن پنجرهٔ گزارش، انتخاب‌های پنجرهٔ اصلی را حفظ می‌کند.

مسیر بازگشت GET امن و مشخص است؛ از `history.back()` کور استفاده نشده تا به POST، چاپ مجدد یا عملیات حذف بازنگردد. در بازشدن مستقیم، نبود history/opener یا بسته‌شدن پنجرهٔ قبلی نیز پیوند جایگزین وجود دارد. نوارها در چاپ مخفی‌اند. خطاهای زودهنگام GET در صفحهٔ چاپ کارنامه، ویرایشگر/چاپ آزمون و گزارش انضباطی نیز صفحهٔ قابل بازگشت دارند؛ قرارداد پاسخ‌های POST/API دست‌نخورده است.

سه مسیر مفقود قبلی (`import.php`، `import-photos.php` و `grade-entry-management.php`) اکنون **صفحهٔ بازیابی با وضعیت واقعی HTTP 404 و دکمهٔ بازگشت** دارند. بنابراین کاربر راه خروج دارد؛ اما ابزار ورود کارنامه، ورود عکس و مدیریت ثبت نمرهٔ مفقود **هنوز بازسازی نشده‌اند**. این صفحات عملیات موفق یا قابلیت موجود را وانمود نمی‌کنند.

فایل‌های باینری دانلودی، پنجرهٔ تنظیم چاپ سیستم‌عامل و صفحات خارجی، صفحهٔ HTML برنامه نیستند؛ نوار داخل آن‌ها تزریق نشده است. عملیات مهاجرتی مخرب نیز برای ممیزی بازگشت روی دادهٔ واقعی اجرا نشده‌اند.

## ۵. دسکتاپ: تمام‌اندازه یا Minimize

لانچر جدید پنجره را در **حالت بیشینهٔ قفل‌شده، به اندازهٔ فضای کاری نمایشگر** نگه می‌دارد؛ حالت پنجره‌ای کوچک، تغییر اندازه و Restore به اندازهٔ عادی محدود شده‌اند. Minimize، بازگردانی از نوار وظیفه، بستن و Alt+F4 حفظ می‌شوند. اجرای دوبارهٔ فایل اجرایی، پنجرهٔ موجود را برمی‌گرداند.

این پیاده‌سازی fullscreen بی‌قابِ F11 نیست: نوار عنوان و راه خروج را عمداً حفظ می‌کند تا کاربر گرفتار پنجرهٔ بدون کنترل نشود. تنها دو حالت اندازهٔ مجاز برنامه، بیشینه و Minimize هستند.

کنترل فقط به PID مرورگرِ اختصاصی برنامه و پنجرهٔ اصلی آن محدود است؛ مرورگرهای دیگر و پنجره‌های مالک‌دارِ انتخاب فایل/چاپ دست‌کاری نمی‌شوند. WinEvent تغییرات را دنبال می‌کند. اگر موتور پشتیبانی‌شده اجرا نشود، به‌جای بازکردن مرورگر پیش‌فرضِ قابل کوچک‌شدن، خطای روشن نمایش داده می‌شود؛ باید Edge/Chrome یا موتور موجود تعمیر/نصب شود.

**حد تأیید:** فایل اجرایی Windows x64 واقعاً بازسازی و ساختار PE آن بررسی شده؛ هشت حالت منطق پنجره با mockِ Win32 اجرا شده است. ویندوز بومی در این محیط در دسترس نیست، بنابراین ظاهر دکمه‌های قاب، Snap، چندنمایشگری و رفتار نسخهٔ Edge/Chrome دستگاه شما هنوز تأیید عملی نشده‌اند.

## شواهد آزمون

- **۳۰ بررسی PHP** برای ذخیره، سریال‌های قدیمی، کد قدیمی، صفر ابتدایی، نگهداری اطلاعات در خطا، ستون‌های گزارش و مجوزها.
- **۲۱ بررسی Chromium** برای کیبورد، ویرایش بدون پرش، موبایل/تبلت/دسکتاپ، مرکز هدر، ۵۳ انتخاب گزارش، بستن popup و حفظ انتخاب‌ها، بارگذاری چهار قلم و شکست بارگذاری.
- PDF واقعیِ گزارش با سه قلم همراه و TTF اختصاصی: خانوادهٔ منتخب در لحظهٔ فراخوانی چاپ، وزن‌های بارگذاری‌شده و وجود FontFile2 در PDF بررسی شدند.
- **۳۹۰ حالت چیدمان**: ۷۸ HTML در پنج عرض؛ بدون خروج سند از عرض، بریدگی پوسته، هم‌پوشانی فوتر یا خطای ثبت‌شدهٔ JS. در تمام این حالت‌ها مسیر بازگشت وجود داشت. پنج نمونهٔ Axe نیز بدون تخلف گزارش‌شده بودند؛ این گواهی AAA نیست.
- هر **۳۰ مجموعهٔ بازگشت قبلی** روی سایت و کد وب دسکتاپ موفق؛ سوئیت پوستهٔ دسکتاپ با سیاست جدید ۳۹ بررسی موفق دارد.
- بررسی دوبارهٔ قلم/کپچا و لیست دبیر در Chromium: دو سرستون، حاشیهٔ امن و دقیقاً دو صفحهٔ A4 حفظ شدند.
- **۸ آزمون mockِ سیاست پنجره** و **۶ آزمون بسته‌بندی** موفق. اسکریپت‌های تازه با دستور زبان ES5 بررسی شده‌اند.

[تصویر فرم دسکتاپ](docs/student-workflow/form-desktop.png) · [تصویر فرم موبایل](docs/student-workflow/form-mobile.png) · [دادهٔ ممیزی](docs/student-workflow/audit.json)

آزمون PHP با SQLite یک‌بارمصرف و بدون دادهٔ مدرسهٔ واقعی است. ممیزی عمومی چیدمان از HTML واقعی PHP و پاسخ‌های شبیه‌سازی‌شدهٔ AJAX استفاده می‌کند؛ اثبات اتصال زندهٔ تمام سرویس‌ها نیست. تصاویر حاوی دادهٔ آزمایشی‌اند. Word و چاپگر فیزیکی نیز تأیید بومی نشده‌اند.

### بازتولید

```bash
python3 scripts/stage-student-workflow.py
SITE=.cache/student-workflow/site bash scripts/run-tests.sh
SITE=.cache/student-workflow/site node tests/test-student-workflow.mjs
SITE=.cache/student-workflow/site node tests/test-student-workflow-browser.mjs
SITE=.cache/student-workflow/site node tests/test-ui-page-catalog.mjs
SITE=.cache/student-workflow/site node tests/test-ui-responsive-browser.mjs
python3 tests/test-desktop-window-policy.py
python3 scripts/release-student-workflow.py
python3 tests/test-student-workflow-packaging.py
```

وابستگی‌های PHP-WASM در `tests`، ابزارهای Playwright/Chromium در `.cache/browser` و کتابخانه‌های سیستم Chromium لازم‌اند. ساخت لانچر به `ziglang` و آزمون mock به کامپایلر C نیاز دارد. سازندهٔ اصلاحی، همهٔ آرشیوهای قبلی را پیش/پس از اجرا از نظر بایت‌های تغییرنکرده کنترل می‌کند؛ هیچ سازندهٔ نسخهٔ کامل اجرا نمی‌شود.
