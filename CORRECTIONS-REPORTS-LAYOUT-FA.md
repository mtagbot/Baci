# اصلاح مسیر پوشه و نشانی نرم‌افزار به reports

این اصلاحی **فقط مخصوص نسخهٔ دسکتاپ ویندوز** است. بستهٔ کامل و بسته‌های قبلی بازسازی نشده‌اند؛ سایت از قبل در `reports` قرار دارد و برای این تغییر به اصلاحی سایت نیاز ندارد. نسخهٔ داخلی نیز تغییر نکرده است.

## نتیجه

- پوشهٔ کامل `SchoolDeskPro/www` در اولین اجرای موفق، به `SchoolDeskPro/reports` تغییر نام می‌دهد؛ فایل‌ها دوباره نصب یا با پوشهٔ دیگری ادغام نمی‌شوند.
- نشانی شروع برنامه `http://127.0.0.1:PORT/reports/` می‌شود؛ `PORT` را خود برنامه انتخاب می‌کند.
- نشانی مشترک **`/reports/assets/logo/logo.png`** به فایل **`SchoolDeskPro/reports/assets/logo/logo.png`** می‌رسد.
- **فایل تصویر باید واقعاً در همان مسیر موجود باشد.** این بسته لوگوی اختصاصی شما را از سایت دانلود نمی‌کند و تصویر جایگزین هم روی آن نمی‌نویسد. اگر فایل فقط روی سایت است، همان فایل/پوشهٔ `assets/logo` را یک بار به مسیر متناظر نرم‌افزار کپی کنید. اگر قبلاً زیر `www/assets/logo` بوده، همراه تغییر نام منتقل می‌شود.
- نشانی‌های قدیمیِ فایل‌های موجود، مانند `/assets/...`، `/uploads/...` و `/index.php`، با پاسخ 307 به `/reports/...` هدایت می‌شوند؛ روش POST و query در این هدایت حفظ می‌شوند. نشانی‌های خصوصی یا ناموجود هدایت عمومی ندارند.
- تنظیمات و SQLite تبدیل یا بازنویسی نمی‌شوند. `data`، فایل‌های بارگذاری‌شده، تنظیمات اتصال و پروفایل مرورگر حفظ می‌شوند. سیاست پنجرهٔ تمام‌اندازه/کوچک‌سازی نیز همان قبلی است.

## نصب روی نسخهٔ موجود

1. ابتدا زنجیرهٔ اصلاحی‌های قبلی تا **settings-health** را نصب کنید:
   `Release_V1.0 → svg-responsive → student-workflow → preview-navigation → fast-ui → mobile-hubs → report-tools → settings-health`.
2. برنامه را ببندید و از **کل پوشهٔ SchoolDeskPro** نسخهٔ پشتیبان بگیرید؛ به‌خصوص `data` و پوشهٔ وب همراه `config` و `uploads`.
3. فایل `SchoolDeskPro-FIX-v2.83.0-reports-layout.zip` را استخراج کنید. **محتویات پوشهٔ SchoolDeskPro داخل ZIP** را کنار `SchoolDeskPro.exe` فعلی کپی کنید؛ جایگزینی همان فایل اجرایی را تأیید کنید. پوشهٔ SchoolDeskPro تو‌در‌تو نسازید.
4. قبل از اجرا باید این دو مورد در محل درست باشند:
   ```text
   SchoolDeskPro/
     SchoolDeskPro.exe                 ← فایل اجرایی جدید
     reports-layout-update/router.php ← فایل مرحله‌ای اصلاحی
     www/                             ← تمام فایل‌های فعلی برنامه
     data/                            ← اطلاعات فعلی
     php/                             ← موتور فعلی
   ```
5. فایل اجرایی جدید را اجرا کنید. پس از مهاجرت موفق، `www` به `reports` تغییر نام می‌دهد، router جدید نصب می‌شود و فایل مرحله‌ای مصرف می‌شود. نسخهٔ router قبلی در `data/router-before-reports.php` نگهداری می‌شود. خالی‌ماندن پوشهٔ `reports-layout-update` اشکالی ندارد.
6. صفحهٔ ورود، نشان مدرسه و یک صفحهٔ ثبت/گزارش را روی ویندوز خود بررسی کنید. پوشهٔ جدید `reports` را دستی دوباره به `www` برنگردانید.

اگر قبلاً **کل** پوشهٔ `www` را دستی به `reports` تغییر نام داده‌اید و دیگر `www` ندارید، همین اصلاحی آن وضعیت را هم می‌پذیرد؛ پوشهٔ خالی یا ناقص کافی نیست.

## توقف‌های محافظتی

- **وجود هم‌زمان www و reports:** برنامه متوقف می‌شود؛ هیچ‌کدام حذف یا با دیگری ادغام نمی‌شوند. نسخهٔ معتبر را با پشتیبان مقایسه کنید. پوشه‌ها را برای ردکردن پیام کورکورانه حذف نکنید.
- **همگام‌سازی قبلی هنوز فعال است:** برنامه نشانگر زنده‌بودن را برمی‌دارد و توقف امن را درخواست می‌کند. اگر قفل worker هنوز در اختیار آن باشد، هیچ تغییر نامی انجام نمی‌شود. پس از پایان درخواست شبکهٔ جاری، کمی صبر و دوباره اجرا کنید؛ worker را وسط نوشتن اطلاعات به اجبار نبندید.
- **خطای دسترسی/فایل ناقص:** سرور بدون router معتبر شروع نمی‌شود. نصب روی مسیر کوتاه، محلی و قابل‌نوشتن انجام شود؛ فایل اجرایی در حال اجرا را جایگزین نکنید.

**اصلاحی‌های قدیمی هنوز مسیر www دارند.** آن‌ها را ترجیحاً پیش از این اصلاحی اعمال کنید. بعد از مهاجرت، ZIP قدیمی را مستقیماً روی ریشهٔ نصب استخراج نکنید، چون دوباره www می‌سازد. اگر واقعاً نیاز به اعمال مجدد فایل‌های وب قدیمی بود، فقط فایل‌های وب موردنظر را با بررسی نسخه داخل `reports` قرار دهید؛ `router.php` یا exe قدیمی را روی نسخهٔ جدید برنگردانید.

## امنیت و حدود آزمون

ریشهٔ PHP برای پشتیبانی واقعی از `/reports/` والد پوشهٔ وب است، اما اجرای **router جدید اجباری** است. مسیرهای `data`، `php`، پیکربندی، پایگاه داده، vendor، فایل‌های داخلی، فایل‌های نقطه‌دار و اسکریپت‌های آپلودی عمومی نمی‌شوند. نصب دارای router قدیمی/نامعتبر با ریشهٔ والد اجرا نمی‌شود.

آزمون‌های ثبت‌شده:
- **44 بررسی مهاجرت** با اجرای کد C اصلی و APIهای فایل/قفل شبیه‌سازی‌شده روی لینوکس: حفظ فایل‌ها، تعارض پوشه‌ها، تکرار اجرا، worker مشغول، خرابی کپی/تغییر نام/جایگزینی و بازیابی.
- **131 بررسی router** با PHP 8.3 WASM: مسیر اصلی و قدیمی، لوگو، POST/query، نصب اولیه، 404 فایل غایب، منع مسیرهای خصوصی، traversal و symlink.
- **12 بررسی مرورگر/خروجی**: اجرای برنامهٔ واقعی پس از جابه‌جایی در فایل‌سیستم WASM، بارگیری لوگوی نمونه از نشانی مشترک در ورود و پنل مدیر، منابع نسبی و درج تصویر با همان نشانی در PDF واقعی TCPDF. انتقال HTTP مرورگر در این آزمون شبیه‌سازی شده است.
- **8 آزمون سیاست پنجره** با Win32 mock؛ خروجی exe نیز PE x86-64 ویندوز با زیرسامانهٔ GUI است.
- تمام **145 ZIP قبلی** هنگام ساخت، بدون تغییر بایت باقی ماندند.

**اجرای بومی روی ویندوز، تعامل واقعی LockFileEx با PHP ویندوز و سرور داخلی PHP بومی در این محیط آزمایش نشده‌اند.** آزمون‌های WASM و mock جایگزین این تأیید نهایی روی دستگاه نیستند. پایگاه دادهٔ واقعی شما نیز در دسترس نبوده و تغییر نکرده است.

## برای نگهداری سورس

- ساخت همین اصلاحی: `python3 scripts/release-reports-layout.py`؛ نیازمند `ziglang` در `.cache/toolchain` یا PYTHONPATH.
- سورس launcher: `desktop-app-v2/launcher/launcher.c` و `reports-layout.h`.
- router جدید: `desktop-app-v2/patch/reports-router.php`.
- شواهد: `docs/reports-layout/`؛ checksum: `REPORTS-LAYOUT-SHA256SUMS.txt`.
- سازنده‌های ZIP قدیمی/کامل تاریخی‌اند؛ در این تغییر اجرا نشده‌اند. برای بازسازی کامل آینده باید چیدمان و تست‌هایشان جداگانه به reports به‌روز شود؛ ترکیب launcher جدید با www/router قدیمی بدون payload مرحله‌ای مجاز نیست.
