# صف ماندگار اعلان‌های تلگرام و بله

## علت مشکل

انتقال رکورد مدرسه با همگام‌سازی و ارسال اعلان ربات دو مسیر جدا بودند. مثلاً ثبت مورد انضباطی، رکورد را وارد دیتابیس می‌کرد، اما اعلان همان لحظه با درخواست مستقیم به ربات فرستاده می‌شد. هنگام قطعی، خطای API ثبت می‌شد و اعلان در صف قابل‌تلاش‌مجدد باقی نمی‌ماند. انتقال بعدی رکورد به سایت نیز تابع اعلان را دوباره اجرا نمی‌کرد.

## رفتار جدید

- اعلان ابتدا با شناسهٔ تصادفی ثابت، متن، پیام‌رسان، حساب گیرنده و دکمه‌هایش در دیتابیس ذخیره می‌شود؛ خطای شبکه موجب حذف آن نمی‌شود.
- مسیر مشترک `bot_send_message` برای **اعلان‌های متنی همهٔ نقش‌ها** اصلاح شده است: انضباط، حضور/غیبت/تأخیر، اعتراض نمره و پاسخ‌ها، اطلاع‌رسانی طراحی آزمون به معاون اجرایی، مشاوره، اعلان‌های دبیران و ارسال متنی هدفمند.
- انتخاب گیرندگان و گزینه‌های فعال/غیرفعال ارسال تغییر نکرده‌اند. حساب گیرنده باید به ربات متصل باشد؛ این اصلاحی برای کاربران فاقد حساب متصل، حساب یا گیرندهٔ ساختگی نمی‌سازد.
- در دسکتاپ، ثبت اعلان هیچ درخواست شبکه‌ای به ربات اجرا نمی‌کند؛ کاربر برای هر گیرنده پشت timeout نمی‌ماند.
- اگر همگام‌سازی هنگام ایجاد اعلان فعال باشد، مالک ارسال **سایت** است. پس از تأیید همگام‌سازی داده‌ها و خالی‌شدن تغییرات معوق، worker دسکتاپ اعلان را به سایت تحویل می‌دهد. تمام شدن ارسال در سایت با رسید جداگانه تأیید می‌شود.
- قطع ارتباط هنگام تحویل به سایت، شناسه را عوض نمی‌کند. درخواست تکراری فقط همان رکورد صف را پیدا می‌کند؛ پیام‌رسان نیز فقط برای رکوردهای تأییدنشده فراخوانی می‌شود.
- اگر اعلان در دسکتاپِ فاقد همگام‌سازی ایجاد شده باشد، worker محلی ارسال آن را پیگیری می‌کند. مالکیت اعلان‌های قدیمی خودسرانه با تغییر تنظیمات جابه‌جا نمی‌شود؛ این کار مانع ارسال هم‌زمان از دو طرف می‌شود.
- در سایت نیز همهٔ گیرندگان ابتدا وارد صف می‌شوند؛ سپس worker محدود انتهای درخواست/worker دوره‌ای ارسال می‌کند. خرابی اتصالِ گیرندهٔ اول، از صف‌گرفتن دیگران جلوگیری نمی‌کند.
- خطاهای شبکه با فاصلهٔ افزایشی تا حدود یک دقیقه دوباره امتحان می‌شوند. خطاهای تنظیمات/گیرنده مانند توکن نامعتبر یا مسدودشدن ربات فاصلهٔ بیشتری، حداکثر یک ساعت دارند. محدودیت `retry_after` پیام‌رسان و توقف موقت همان پیام‌رسان رعایت می‌شود؛ خطای بله مانع پردازش تلگرام نمی‌شود.
- فایل‌های درخواستی ربات، PDF، عکس و ویدئو همچنان مسیر ارسال قبلی خود را دارند؛ این اصلاحی صف **اعلان‌های متنی** است، نه تبدیل همهٔ دانلودها و انتقال رسانه به سامانهٔ انتقال فایل پس‌زمینه.

## نصب — هر دو طرف لازم است

ابتدا نسخهٔ سایت و نرم‌افزار باید اصلاحی‌های قبلی تا **settings-health** را داشته باشند. در دسکتاپ، اصلاحی **reports-layout** نیز باید با موفقیت اعمال شده باشد؛ مقصد این بسته `reports` است، نه `www`.

۱. از دیتابیس و فایل‌های سایت و کل پوشهٔ نرم‌افزار پشتیبان بگیرید.

۲. **ابتدا سایت:** محتوای پوشهٔ `site-update-v4.152.0` داخل `SITE-FIX-v4.152.0-bot-outbox.zip` را داخل `reports` سایت، کنار `index.php` فعلی، کپی کنید. این اصلاحی `desk-sync-api.php` اصلی، فایل کلید، توکن ربات یا تنظیمات دیتابیس را جایگزین نمی‌کند. کلید خصوصی Release_V1.0 و نصب‌های قدیمی با کلید معتبر قبلی پشتیبانی می‌شوند.

۳. **سپس نرم‌افزار:** برنامه را کامل ببندید و پوشهٔ `SchoolDeskPro` داخل `SchoolDeskPro-FIX-v2.83.0-bot-outbox.zip` را با محل نصب هم‌مسیر کنید. شش فایل وب زیر `SchoolDeskPro/reports` جایگزین/اضافه می‌شوند؛ exe و بستهٔ کامل بازسازی نشده‌اند. اگر هنوز `www` دارید، ابتدا مهاجرت reports را انجام دهید؛ یک reports ناقص کنار www نسازید.

۴. **worker قدیمی باید خارج شود.** پس از کپی، برای اطمینان از بارگذاری worker جدید یک بار ویندوز را Restart کنید. صرف بستن و سریع بازکردن پنجره همیشه کافی نیست؛ worker قبلی ممکن است مدتی زنده بماند. پردازش همگام‌سازی را وسط نوشتن اطلاعات به اجبار terminate نکنید.

۵. آدرس همگام‌سازی سایت برای تحویل اعلان‌ها باید **HTTPS با گواهی معتبر** باشد. مسیر جدید صف، اعتبار TLS را بررسی می‌کند و به HTTP تنزل نمی‌دهد یا به میزبان دیگری redirect نمی‌شود. اگر اطلاعات همگام می‌شوند ولی صف تحویل نمی‌شود، آدرس نهایی HTTPS، گواهی/CA نرم‌افزار، کلید و نصب اصلاحی سایت را بررسی کنید.

## Cron سایت — برای ادامهٔ ارسال بدون نرم‌افزار باز

در بخش Cron Jobs هاست، فرمان زیر را **هر دقیقه** اجرا کنید:

```text
* * * * * /usr/bin/php /home/ACCOUNT/public_html/reports/cron/bot-outbox-worker.php
```

هر دو مسیر نمونه‌اند: مسیر PHP CLI و مسیر فایل را از هاست خود بگیرید. همین PHP باید به دیتابیس نصب دسترسی داشته و افزونه‌های موردنیاز برنامه، از جمله cURL، PDO و mbstring را داشته باشد. worker فقط از خط فرمان اجرا می‌شود و کلید یا توکن در فرمان Cron قرار نمی‌گیرد. این Cron **جایگزین دریافت پیام‌های ورودی ربات نیست**؛ تنظیم Webhook/telegram-poll قبلی را حفظ کنید.

هنگامی که نرم‌افزار باز و همگام‌سازی برقرار است، تحویل به سایت خودش پردازش محدود صف سایت را هم فعال می‌کند. Cron برای تخلیهٔ مستقل و پیوستهٔ صفِ تحویل‌شده پس از بسته‌شدن نرم‌افزار ضروری است. پیام‌هایی که هنوز فقط روی دستگاه آفلاین‌اند، طبیعتاً تا روشن/بازشدن نرم‌افزار و برقراری اتصال به سایت قابل انتقال نیستند.

## دیدن وضعیت

در صفحهٔ مدیریت هر یک از ربات‌های بله و تلگرام، بخش «صف ماندگار اعلان‌ها» اضافه شده است:

- **در انتظار تلاش:** اعلان ذخیره شده، اما تحویل تأیید نشده است.
- **در حال ارسال:** worker قفل موقت ارسال را در اختیار دارد.
- **در صف سایت:** سایت اعلان را دریافت کرده، اما پیام‌رسان هنوز ارسال را تأیید نکرده است.
- **ارسال تأییدشده:** پاسخ موفق پیام‌رسان دریافت شده است؛ به معنی خواندن پیام توسط کاربر نیست.

آخرین خطاهای صف، شمارش وضعیت‌ها و دکمهٔ «آماده‌سازی صف برای تلاش مجدد» نیز وجود دارد. این دکمه فقط زمان تلاش اعلان‌های معوق را آماده می‌کند؛ رکورد ارسال‌شده را دوباره نمی‌فرستد و محدودیت زمانی اعلام‌شدهٔ پیام‌رسان یا قفل فعال worker را دور نمی‌زند. بعد از اصلاح توکن می‌توانید از آن استفاده کنید. در دسکتاپ، وضعیت‌های سایت ممکن است با تأخیر کوتاهِ دریافت رسید به‌روز شوند.

سبزشدن همگام‌سازی اطلاعات لزوماً به معنی ارسال همهٔ اعلان‌ها نیست؛ وضعیت صف ربات را جدا ببینید. در صورت مسدودبودن Telegram API روی هاست، همچنان باید واسط معتبر تلگرام یا دسترسی مناسب هاست فراهم شود؛ صف نمی‌تواند محدودیت شبکه، توکن نادرست یا مسدودکردن ربات توسط گیرنده را برطرف کند.

## حفظ داده و محدودیت‌های واقعی

دو جدول خصوصی `bot_outbox` و `bot_outbox_limits` در همان دیتابیس موجود، بدون تبدیل SQLite/MySQL، ساخته می‌شوند. این جدول‌ها وارد snapshot/reconcile عمومی همگام‌سازی نمی‌شوند؛ دریافت مجدد اطلاعات مدرسه نباید صف معوق یا رسید ارسال را حذف کند. قفل ارسال با UPDATE شرطی اتمی و lease انجام می‌شود؛ worker متوقف‌شده پس از انقضای lease قابل بازیابی است. DDL روی MySQL در تراکنش بازِ عملیات مدرسه اجرا نمی‌شود.

این اصلاحی از زمان نصب، اعلان‌های جدید را ماندگار می‌کند. **اعلان‌های قدیمی که قبلاً اصلاً در صف ذخیره نشده‌اند، قابل بازیابی تضمینی نیستند.** فقط از وجود رکورد انضباطی نمی‌توان فهمید کدام گیرنده قبلاً پیام را دریافت کرده است؛ ارسال انبوه مجددِ تاریخچه به‌صورت خودکار انجام نمی‌شود.

رسیدهای تحویل به سایت، قفل worker و وضعیت sent جلوی تکرار معمول را می‌گیرند. اما API ربات‌ها تضمین سراسری exactly-once نمی‌دهد: اگر پیام‌رسان پیام را پذیرفته باشد و پاسخ آن گم شود یا برنامه درست پیش از ثبت موفقیت متوقف شود، تلاش بعدی ممکن است یک پیام تکراری بسازد. تضمین «هیچ تکراری تحت هیچ خرابی» داده نمی‌شود.

## بررسی‌های انجام‌شده

- ۳۴ بررسی سایت و ۴۵ بررسی دسکتاپ/تحویل به endpoint واقعی در PHP 8.3 WASM و SQLite: چند گیرنده و هر دو پیام‌رسان، نقش‌های کارکنان، قطعی، اتصال مجدد، ماندگاری بین درخواست‌ها، رسید گم‌شده، شناسهٔ تکراری/متعارض، قفل فعال و منقضی، محدودیت نرخ و استقلال پیام‌رسان‌ها، توکن نامعتبر، مخفی‌کردن توکن در خطا، rollback، خطای ذخیره‌سازی و عدم گزارش موفقیت کاذب، احراز هویت endpoint و کلید خصوصی/قدیمی.
- ۳۱ سوئیت رگرسیون قبلی برای هر توزیع، از جمله آزمون‌ها، گروه کلاس، چاپ، ورود و وضعیت اتصال.
- اطلاعات واقعی مدرسه و توکن واقعی ربات استفاده نشد. پاسخ‌های تلگرام/بله و قطعی شبکه در آزمون‌ها شبیه‌سازی شده‌اند؛ ارسال واقعی به کاربران، MySQL بومی و اجرای worker در ویندوز/هاست مشتری در این محیط آزموده نشده است.

شواهد در `docs/bot-outbox` و checksumها در `BOT-OUTBOX-SHA256SUMS.txt` ثبت می‌شوند. فقط جفت ZIP اصلاحی جدید ساخته شده است؛ فایل‌های کامل و اصلاحی‌های تاریخی دست‌نخورده‌اند.
