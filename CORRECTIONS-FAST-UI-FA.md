# اصلاح فوتر، پیام‌های ورود، نمایش اولیه و وضعیت اتصال دسکتاپ

**فقط بستهٔ اصلاحی — نسخه‌های کامل Release_V1.0 و فایل اجرایی ویندوز بازسازی نشده‌اند.** شماره‌های داخلی سایت 4.152.0 و دسکتاپ 2.83.0 ثابت هستند.

## دانلود و ترتیب نصب

- [اصلاحی سایت، ۲۱ فایل، حدود ۲۲۵ کیبی‌بایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SITE-FIX-v4.152.0-fast-ui.zip)
- [اصلاحی دسکتاپ، ۲۲ فایل، حدود ۲۲۷ کیبی‌بایت](https://github.com/mtagbot/Baci/raw/refs/heads/arena/01a0a1d9-baci/SchoolDeskPro-FIX-v2.83.0-fast-ui.zip)
- [SHA256](FAST-UI-SHA256SUMS.txt) · [فهرست ۲۱ فایل مشترک](scripts/fast-ui-files.json)

این بسته **پس از اصلاحی preview-navigation** نصب شود؛ جایگزین نصب کامل یا مجموعهٔ اصلاحات قبلی نیست. ترتیب برای نصب از مبنای موجود: Release_V1.0 ← svg-responsive ← student-workflow ← preview-navigation ← fast-ui.

۱. از نصب و داده‌ها پشتیبان بگیرید.
۲. سایت: محتویات پوشهٔ `site-update-v4.152.0` را در ریشهٔ نصب موجود ادغام و فایل‌های هم‌نام را جایگزین کنید.
۳. دسکتاپ: برنامه را کاملاً ببندید؛ محتویات `SchoolDeskPro/www` را با `www` موجود ادغام کنید، سپس برنامه را دوباره اجرا کنید. فایل اضافی دسکتاپ، `desk-sync-daemon.php` است؛ شروع مجدد برای بارگذاری کد تازهٔ سرویس پس‌زمینه لازم است.
۴. اگر هاست OPcache را بدون بررسی تغییر فایل نگه می‌دارد، آن را بازنشانی کنید. کلید کش منابع مشترک تغییر کرده است؛ در صورت باقی‌ماندن ظاهر قبلی، صفحه را نوسازی کامل کنید.

هیچ داده، رمز، کلید اتصال، تصویر یا قلم بارگذاری‌شده‌ای در این اصلاحی جایگزین نمی‌شود. فونت‌های جدید فقط نسخهٔ فشردهٔ قلم‌های موجود در بستهٔ مبنا هستند و زیر `assets/fonts/screen` قرار دارند؛ پوشهٔ `uploads` دست‌نخورده می‌ماند. همهٔ ZIPهای قبلی با آزمون بایتی کنترل شده‌اند و تغییر نکرده‌اند.

## ۱. فوتر جدید

- فوتر قبلیِ چندبخشی با یک نوار کم‌ارتفاع جایگزین شد: نام مدرسه، اعتبار طراحی و پیوند نشست‌های کاربر.
- فوتر سایت در جریان عادی صفحه است. در دسکتاپ، نوار پایین ثابت است و از ابتدا برای آن فضا رزرو می‌شود تا آخرین ردیف و دکمهٔ فرم زیر آن نماند.
- در عرض کم چیدمان جمع می‌شود. پنل جداگانهٔ ناوبری یا دکمهٔ بازگشت عمومی اضافه نشده است.

## ۲. پیام‌های دانش‌آموز، دبیر و استعلام

- مشکل نمایش پیام برای مهمان برطرف شد: پیام در همان فرم و تبِ درخواست ناموفق نمایش داده می‌شود، نه در ناحیهٔ مخصوص کاربران واردشده.
- خطای اطلاعات ورود، فیلد خالی، اعتبار تمام‌شدهٔ فرم، کپچای نادرست و محدودیت تعداد تلاش‌ها، راهنمای فارسی مشخص دارند.
- خطا با `role=alert`، فوکوس قابل دسترس و ارتباط با فیلدهای نامعتبر نمایش داده می‌شود؛ خودکار پس از چند ثانیه ناپدید نمی‌شود.
- فقط شناسهٔ واردشده برای یک بار نگه داشته می‌شود؛ رمز، سریال و پاسخ کپچا در فرم بعدی بازنویسی نمی‌شوند.
- پیام حساب ناموجود و غیرفعال با خطای اطلاعات ورود یکسان است؛ وجود حساب افشا نمی‌شود.
- برچسب‌ها دیگر القا نمی‌کنند که رمز همهٔ دانش‌آموزان الزاماً شش رقم یا رمز همهٔ دبیران الزاماً کد پرسنلی است. شیوه‌های قبلی اعتبارسنجی رمز و سریال حفظ شده‌اند؛ دسترسی تازه‌ای با میان‌بر رمز ایجاد نشده است.
- تلاش اول همچنان بدون کپچای مؤثر است؛ خطای اطلاعات ورود، کپچای تلاش بعدی را فعال می‌کند. نبودن کارنامه پس از ورود معتبر، خطای رمز تلقی نمی‌شود.

[نمونهٔ پیام ورود دبیر با دادهٔ آزمایشی](docs/fast-ui/teacher-login-error.png)

## ۳. سرعت و ثبات نمایش اولیه

علت مهم جهش در آزمون سرد، تغییر اندازهٔ متنِ قلم جایگزین و شکستن دکمه‌های عملیات به چند خط بود: ارتفاع بعضی ردیف‌های دانش‌آموز از حدود ۱۰۸٫۵ به ۶۵ پیکسل تغییر می‌کرد.

اصلاحات انجام‌شده:

- حذف انیمیشن ورود صفحه/فرم و زمان‌بندی غیرضروری نمایش ردیف‌ها؛
- تثبیت عرض کاشی‌های هدر، دکمه‌های اصلی و ستون‌ها/ردیف‌های جدول دانش‌آموز؛ نام‌های بلند بدون حذف محتوا امکان چندخطی‌شدن دارند؛
- استفاده از همان محفظهٔ جدولِ رندرشده در HTML، به‌جای افزودن محفظهٔ تو‌در‌تو بعد از نمایش؛
- انتقال CSS فرم دانش‌آموز به `<head>` و پشتیبانی صحیح از چند stylesheet صفحه؛
- حذف تعریف‌های تکراری قلم و پیش‌بارگذاری فقط قلم انتخابی؛ WOFF2 برای قلم‌های اصلی با مسیر TTF جایگزین برای مرورگر فاقد پشتیبانی؛
- تطبیق هش قلم اصلی قبل از انتخاب فایل فشرده: قلم اختصاصی یا فایل اصلیِ تغییرکرده بی‌اجازه با نسخهٔ بسته جایگزین نمی‌شود؛
- حذف دانلود Chart از صفحه‌هایی که نمودار ندارند؛ بارگذاری deferred اسکریپت‌های عمومی؛
- ساخت گزینه‌های پنل جستجوی فهرست بلند فقط هنگام بازشدن آن، نه ساخت صدها عنصر مخفی در شروع صفحه.

صفحه تا اتمام همهٔ منابع پشت پرده یا صفحهٔ بارگذاری مخفی نمی‌شود. دریافت قلم اختصاصیِ تازه و داده از شبکه زمان می‌خواهد؛ ادعای نمایش آنی روی هر اینترنتی نداریم. **انتظار و کنترل خطای قلم در چاپ نهایی گزارش دانش‌آموز و قلم‌های اصلی PDF/Word حفظ شده است.**

### اندازه‌گیری قابل بازبینی

Chromium، عرض ۱۴۴۰، کندسازی CPU چهاربرابری؛ هر صفحه در context تازه، تأخیر مصنوعی قلم ۷۰۰ms، CSS/JS برابر ۱۵۰ms و Chart برابر ۶۰۰ms. پاسخ‌های پس‌زمینهٔ PHP شبیه‌سازی شده‌اند. این اعداد یک نمونهٔ آزمایشگاهی‌اند، نه زمان پاسخ هاست واقعی یا ویندوز کاربر.

| صفحه | آماده‌شدن DOM قبل ← بعد، ms | جابه‌جایی چیدمان CLS قبل ← بعد | منابع درخواستی قبل ← بعد، KiB |
|---|---:|---:|---:|
| ورود مهمان | ۷۵۳ ← ۴۲۰ | ۰٫۰۰۰۰۱۴ ← ۰٫۰۰۰۵۶۰ | ۶۰۳٫۳ ← ۳۸۷٫۰ |
| داشبورد | ۷۱۴ ← ۴۹۶ | ۰٫۰۰۷۶۹۱ ← ۰٫۰۰۰۹۳۶ | ۶۰۰٫۰ ← ۳۸۲٫۷ |
| فهرست دانش‌آموز | ۷۷۷ ← ۵۴۲ | ۰٫۰۶۵۳۸۴ ← ۰٫۰۰۰۷۸۴ | ۶۰۲٫۴ ← ۳۸۵٫۲ |

در این نمونه، منابع درخواستی تقریباً **۳۶٪ کمتر** و CLS فهرست دانش‌آموز تقریباً **۹۹٪ کمتر** شد. CLS صفحهٔ ورود از مقدار بسیار کوچک قبلی کمی بیشتر شده است؛ صفر مطلق ادعا نمی‌شود. FCP فهرست دانش‌آموز نیز از ۴۰۴ به ۵۰۰ms رسید؛ بنابراین ادعا نمی‌کنیم تک‌تک شاخص‌ها در همهٔ صفحه‌ها سریع‌تر شده‌اند. بهبود اصلی این صفحه، آماده‌شدن زودتر و حذف جهش بزرگ ردیف‌هاست. حجم منابع، مجموع فایل‌های دریافت‌شده در آزمون است و معادل بایت فشردهٔ روی سیم نیست.

[دادهٔ خام اندازه‌گیری و منابع جابه‌جایی](docs/fast-ui/performance.json)

## ۴. نشانگر اتصال دسکتاپ

نشانگر کوچکِ پایین برنامه، با حلقهٔ چرخان هنگام بررسی/کار سرویس و جزئیات بازشونده، حالت‌های زیر را جدا می‌کند:

- بررسی اولیه، همگام‌سازی در حال اجرا، تغییرات در صف؛
- همگام با سرور پس از پاسخ معتبر و نبود تغییر ارسال‌نشده؛
- انتظار برای اتصال/تلاش مجدد، خطای همگام‌سازی، هشدار همگام‌سازی ناقص، گزارش قدیمی یا نامشخص؛
- همگام‌سازی غیرفعال، شبکهٔ دستگاه قطع، یا نیاز به ورود.

**آنلاین‌بودن مرورگر به‌تنهایی به معنی اتصال اینترنت یا همگام‌بودن نیست.** جزئیات، وضعیت شبکهٔ دستگاه را از پاسخ معتبر سرور مدرسه و زمان آخرین همگام‌سازی موفق جدا می‌کند. خرابی سرور، کلید اشتباه یا پاسخ نامعتبر با «اینترنت حتماً قطع است» اشتباه گرفته نمی‌شود. نتیجهٔ `idle` از سرویس قدیمی، قفل، backoff یا گزارش مانده، به‌اشتباه چراغ موفقیت نمی‌گیرد.

- بررسی رابط فقط فایل وضعیت محلی و بانک اطلاعاتی محلی را می‌خواند؛ **دیگر tick شبکه‌ای قدیمی را از فوتر اجرا نمی‌کند** و در نبود daemon نیز داخل درخواست صفحه منتظر شبکه نمی‌ماند.
- فقط سرویس پس‌زمینه کار ارتباطی را انجام می‌دهد؛ سیاست ارسال/دریافت و تلاش مجدد آن حفظ شده است. شروع و پایان فعالیت را در heartbeat ثبت می‌کند.
- یک درخواست غیرهم‌پوشان در هر ۵ ثانیه؛ در خطای خواندن وضعیت، فاصله ۱۵ ثانیه است. تب پنهان بررسی را متوقف می‌کند، iframe داخلی پایش جدا نمی‌سازد و مهمان درخواست وضعیت نمی‌فرستد.
- حلقه در وضعیت سالم یا انتظار بین تلاش‌ها بی‌جهت نمی‌چرخد؛ کاهش حرکت سیستم و پنهان‌شدن صفحه رعایت می‌شود. بررسی پس‌زمینه نوار بارگذاری عمومی صفحه را روشن نمی‌کند.
- شمار تغییرات ارسال‌نشده فقط به مدیر داده می‌شود؛ کلید، URL خصوصی و متن خام خطای سرویس در پاسخ نشانگر نیست.
- هشدار خروج با تغییرات ارسال‌نشدهٔ شناخته‌شده حفظ شده است؛ محدودیت‌های اعلان `beforeunload` مرورگر همچنان برقرارند.

[تصویر نشانگر دسکتاپ و جزئیات آن](docs/fast-ui/desktop-connection.png)

## تأیید و حدود آن

- **۳۱ مجموعه‌آزمون روی سایت و ۳۱ مجموعه روی سورس دسکتاپ موفق**؛ شامل ۱۱۷ بررسی تازهٔ PHP برای ورود، قلم، وضعیت اتصال، سطح دسترسی و پرچم تأیید واقعی سرور.
- **۹۲ بررسی مرورگری**: خطاهای سه فرم، ماندگاری پیام، قلم‌ها، حالت‌های نشانگر، توقف بررسی در پس‌زمینه/خروج از نشست، کاهش حرکت، عرض‌های مختلف، هشدار خروج، ۵۰۰ ردیف داده و فهرست جستجوی ۵۰۰گزینه‌ای.
- **۳۹۰ حالت چیدمان، ۷۸ درخواست HTML در پنج عرض**: بدون خروج سند از عرض و بدون هم‌پوشانی فوتر سایت؛ نمونه‌های Axe بدون تخلف گزارش‌شده. وضعیت فوتر ثابت دسکتاپ نیز جداگانه در پنج عرض بررسی شده است.
- **۷۹ بررسی محدودهٔ بازگشت** و **۲۱ بررسی فرم/کیبورد/قلم گزارش** موفق؛ بازگشت عمومی به صفحات عادی برنگشته است.
- کنترل مرورگری چاپ: قلم‌های انتخابی، کپچا، دو سرستون نام و دقیقاً دو صفحهٔ A4 فهرست دبیر حفظ شدند.
- ۵ آزمون بسته‌بندی جدید و ۴ آزمون بستهٔ قبلی؛ نحو ES5 پنج اسکریپت تغییرکرده بررسی شد.

این تأییدها شامل شبکهٔ واقعی مدرسه، MySQL هاست کاربر، اجرای GUI ویندوز، چاپگر فیزیکی، Word بومی یا تضمین همهٔ مرورگرهای قدیمی نیست. نمایش چند حالت نشانگر با پاسخ‌های کنترل‌شده آزمایش شده است؛ ظاهر چرخان به‌تنهایی مدرک اتصال نیست.

### تکرار آزمون

```sh
python3 scripts/stage-fast-ui.py
python3 scripts/stage-fast-ui.py --desktop
SITE=.cache/fast-ui/site bash scripts/run-tests.sh
SITE=.cache/fast-ui/desktop/SchoolDeskPro/www bash scripts/run-tests.sh
SITE=.cache/fast-ui/site node tests/test-ui-page-catalog.mjs
LD_LIBRARY_PATH=$PWD/.cache/browser/lib/lib SITE=.cache/fast-ui/site node tests/test-fast-ui-browser.mjs
python3 tests/test-fast-ui-packaging.py
```

برای بازسازی مبنای مقایسه و اجرای سنجش عملکرد:

```sh
python3 scripts/stage-fast-ui.py --before
SITE=.cache/speed-audit/before UI_CATALOG_ROOT=.cache/speed-audit/before-catalog node tests/test-ui-page-catalog.mjs
SITE=.cache/fast-ui/site node tests/test-ui-page-catalog.mjs
LD_LIBRARY_PATH=$PWD/.cache/browser/lib/lib SITE=.cache/fast-ui/site node tests/test-fast-ui-performance.mjs
```

مبنای قبلی فقط از ZIPهای منتشرشده ساخته می‌شود. مسیرها و شرایط در `tests/test-fast-ui-performance.mjs` مشخص‌اند. ابزار تبدیل قلم فقط زمان ساخت به `fonttools` و `brotli` نیاز دارد؛ نصب کاربر به آن‌ها وابسته نیست.
